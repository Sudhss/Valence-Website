/* Valence — the actual source of the editor, as rendered by the buffer scene.
 *
 * Generated from the repository, not written by hand and not invented. These
 * are the real files in the real order the architecture section describes, with
 * tabs expanded, trailing whitespace stripped, lines over 118 columns truncated,
 * and non-ASCII replaced so the glyph atlas has something to draw.
 *
 * The scene tokenizes this at runtime with js/lexer.js — the port of the
 * editor's own cpp_highlighter.cpp. So the page colours Valence's source using
 * Valence's own lexer.
 */

export const SOURCE_FILES = [
{
"name": "src/theme/theme.h",
"start": 0,
"count": 221
},
{
"name": "src/core/selection.h",
"start": 221,
"count": 33
},
{
"name": "src/core/text_buffer.h",
"start": 254,
"count": 41
},
{
"name": "src/core/text_buffer.cpp",
"start": 295,
"count": 217
},
{
"name": "src/core/undo_manager.h",
"start": 512,
"count": 57
},
{
"name": "src/core/undo_manager.cpp",
"start": 569,
"count": 128
},
{
"name": "src/editor/cpp_highlighter.h",
"start": 697,
"count": 32
},
{
"name": "src/editor/cpp_highlighter.cpp",
"start": 729,
"count": 195
},
{
"name": "src/editor/snippets.h",
"start": 924,
"count": 53
},
{
"name": "src/editor/editor_widget.h",
"start": 977,
"count": 200
},
{
"name": "src/editor/editor_widget.cpp",
"start": 1177,
"count": 1327
},
{
"name": "src/cph/test_case.h",
"start": 2504,
"count": 53
},
{
"name": "src/cph/judge.h",
"start": 2557,
"count": 72
},
{
"name": "src/cph/judge.cpp",
"start": 2629,
"count": 340
},
{
"name": "src/ui/cph_panel.h",
"start": 2969,
"count": 48
},
{
"name": "src/ui/test_case_card.h",
"start": 3017,
"count": 64
},
{
"name": "src/ui/terminal_widget.h",
"start": 3081,
"count": 67
},
{
"name": "src/ui/file_explorer.h",
"start": 3148,
"count": 60
},
{
"name": "src/ui/tab_widget.h",
"start": 3208,
"count": 41
},
{
"name": "src/ui/status_bar.h",
"start": 3249,
"count": 28
},
{
"name": "src/ui/main_window.h",
"start": 3277,
"count": 96
},
{
"name": "src/ui/main_window.cpp",
"start": 3373,
"count": 765
},
{
"name": "src/main.cpp",
"start": 4138,
"count": 72
}
];

export const SOURCE_LINES = ["#pragma once", "#include <QColor>", "#include <QFont>", "#include <QString>", "#include <QStringList>", "", "namespace Theme {", "    // -- Elevation --------------------------------------------------------", "    //", "    // A dark interface reads as flat plastic when every surface sits within a", "    // few points of every other one, which is exactly what this palette used to", "    // do: five \"different\" backgrounds spanning ten RGB values, butted together", "    // with identical hairlines. Depth in a dark UI comes from a deliberate", "    // ramp - the further forward a surface is, the lighter it sits - plus a", "    // highlight along the top edge of raised surfaces, as if lit from above.", "    //", "    // The neutrals carry a slight blue-violet cast rather than being pure grey;", "    // a perfectly neutral dark reads as cheap, and the tint gives the teal", "    // accent something to sit against.", "    inline const QColor Base    ( 11,  12,  16);   // #0b0c10 app floor, terminal", "    inline const QColor Chrome  ( 16,  18,  24);   // #101218 sidebar, status, tab strip", "    inline const QColor Surface ( 21,  23,  29);   // #15171d the editor itself", "    inline const QColor Raised  ( 27,  30,  38);   // #1b1e26 cards, active tab, panels", "    inline const QColor Overlay ( 34,  38,  47);   // #22262f menus, popups - floats above all", "", "    // Existing names, remapped onto the ramp so every call site moves at once.", "    inline const QColor EditorBg   = Surface;", "    inline const QColor SidebarBg  = Chrome;", "    inline const QColor TerminalBg = Base;", "    inline const QColor TitlebarBg = Chrome;", "    inline const QColor PanelBg    = Chrome;", "", "    // -- Accent --", "    //", "    // The signature was #00ff9c: fully saturated, red channel at zero. Neon on", "    // near-black is the house style of terminal-hacker themes, and it is the", "    // loudest thing in the interface by a wide margin. This is the same teal,", "    // pulled back from fluorescent to something with a bit of depth - still", "    // unmistakably the brand, no longer shouting.", "    //", "    // To restore the original exactly, set Accent = AccentNeon.", "    inline const QColor AccentNeon(  0, 255, 156);         // #00ff9c - the original", "    inline const QColor Accent    ( 47, 224, 160);         // #2fe0a0", "    inline const QColor AccentHot ( 92, 240, 186);         // #5cf0ba - hover / focus", "    inline const QColor AccentGlow( 47, 224, 160,  64);", "    inline const QColor AccentDim ( 47, 224, 160,  30);", "    inline const QColor AccentWash( 47, 224, 160,  18);    // large fills only", "    inline const QColor AccentBlue( 96, 165, 250);         // #60a5fa - secondary", "    inline const QColor OnAccent  (  8,  14,  12);         // text drawn ON the accent", "", "    // -- Text --", "    // Four steps, not three: headings, body, labels, and the things that should", "    // barely register until you look for them.", "    inline const QColor TextPrimary  (233, 236, 241);      // #e9ecf1", "    inline const QColor TextSecondary(233, 236, 241, 160);", "    inline const QColor TextMuted    (233, 236, 241, 102);", "    inline const QColor TextFaint    (233, 236, 241,  58);", "", "    // -- Syntax --", "    inline const QColor SynKeyword(86, 209, 255);          // #56d1ff - vivid sky blue", "    inline const QColor SynType(130, 170, 255);            // #82aaff - periwinkle", "    inline const QColor SynString(195, 232, 141);          // #c3e88d - lime green", "    inline const QColor SynComment(233, 236, 241, 96);     // recedes, but stays readable", "    inline const QColor SynNumber(255, 183, 77);           // #ffb74d - warm orange", "    inline const QColor SynPreprocessor(199, 146, 234);    // #c792ea - purple", "    inline const QColor SynFunction(130, 231, 135);        // #82e787 - bright green", "    inline const QColor SynPunctuation(233, 236, 241, 165); // brackets should not disappear", "", "    // -- Verdict / Judge states (CPH panel, diagnostics) --", "    // Desaturated on purpose: these sit next to code all day and must not shout.", "    inline const QColor Success(126, 211, 141);           // #7ed38d - calm green (AC)", "    inline const QColor SuccessBg(126, 211, 141, 26);", "    inline const QColor Failure(240, 113, 120);           // #f07178 - soft coral (WA)", "    inline const QColor FailureBg(240, 113, 120, 26);", "    inline const QColor Warning(255, 183, 77);            // #ffb74d - amber (TLE)", "    inline const QColor WarningBg(255, 183, 77, 26);", "    inline const QColor Pending(233, 236, 241, 90);       // idle / not yet run", "    inline const QColor PendingBg(255, 255, 255, 12);", "", "    // -- Edges & light --", "    //", "    // A single flat hairline everywhere is what makes panels look stuck onto", "    // each other. Real edges have direction: a raised surface catches light on", "    // its top edge and casts a darker line at the bottom.", "    inline const QColor Border       (255, 255, 255,  16);  // divider between panes", "    inline const QColor BorderMedium (255, 255, 255,  26);  // control outlines", "    inline const QColor BorderStrong (255, 255, 255,  38);  // focused control", "    inline const QColor HighlightTop (255, 255, 255,  20);  // top edge of a raised surface", "    inline const QColor ShadowSoft   (  0,   0,   0,  70);  // under floating things", "    inline const QColor ShadowDeep   (  0,   0,   0, 120);", "", "    // Hover and selection as tokens, so every widget reacts by the same amount.", "    inline const QColor HoverWash    (255, 255, 255,  13);", "    inline const QColor ActiveWash   (255, 255, 255,  22);", "", "    // -- UI --", "    inline const QColor CurrentLine(255, 255, 255, 11);   // present, still quiet", "    inline const QColor SelectionBg(47, 224, 160, 40);", "    inline const QColor ScrollThumb(255, 255, 255, 26);", "    inline const QColor ScrollThumbHover(255, 255, 255, 52);", "", "    // -- Metrics --", "    // One spacing scale for every widget, so panels line up without eyeballing.", "    inline const int SpaceXS = 4;", "    inline const int SpaceS  = 8;", "    inline const int SpaceM  = 12;", "    inline const int SpaceL  = 16;", "    inline const int Radius      = 6;                      // buttons, badges, inputs", "    inline const int RadiusPanel = 9;                      // cards, popups, menus", "    inline const int RowHeight   = 28;                      // tool buttons, list rows", "    inline const int HeaderHeight = 36;                     // panel headers", "", "    // A vertical gradient across a chrome surface is the cheapest convincing", "    // depth cue there is, and Qt stylesheets support it directly.", "    inline QString chromeGradient(const QColor& top, const QColor& bottom) {", "        return QString(\"qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 %1, stop:1 %2)\")", "            .arg(top.name(), bottom.name());", "    }", "", "    // -- Type ---------------------------------------------------------------", "    //", "    // Two typefaces, used for two different jobs. Mixing them up is the single", "    // most common reason a desktop app looks homemade: monospace is for code", "    // and terminal output, and for nothing else. Menus, tabs, labels, buttons", "    // and the status bar all belong to the platform's UI face.", "    //", "    // Families are given as an ordered fallback list rather than one name.", "    // Asking for a font that is not installed does not fail loudly - Qt", "    // silently substitutes whatever it likes, which is how every glyph in this", "    // app ended up in a generic fallback face.", "", "    inline QStringList codeFamilies() {", "        return {", "            QStringLiteral(\"JetBrains Mono\"),   // if the user installs it, it wins", "            QStringLiteral(\"Cascadia Code\"),    // ships with Windows Terminal / VS", "            QStringLiteral(\"Cascadia Mono\"),", "            QStringLiteral(\"Consolas\"),         // always present on Windows", "            QStringLiteral(\"DejaVu Sans Mono\"),", "            QStringLiteral(\"monospace\"),", "        };", "    }", "", "    inline QStringList uiFamilies() {", "        return {", "            QStringLiteral(\"Segoe UI Variable Text\"),   // Windows 11", "            QStringLiteral(\"Segoe UI\"),                 // Windows 10", "            QStringLiteral(\"Inter\"),", "            QStringLiteral(\"system-ui\"),", "            QStringLiteral(\"sans-serif\"),", "        };", "    }", "", "    // Sizes are in logical pixels, not points. Points made the editor render", "    // around 17px on a 96dpi display, which is why everything felt oversized;", "    // pixels say what is meant and still scale with the device pixel ratio.", "    inline const int FontSizeEditor   = 14;", "    inline const int FontSizeTerminal = 13;", "    inline const int FontSizeUI       = 13;", "    inline const int FontSizeSmall    = 12;", "", "    inline QFont codeFont(int pixelSize = FontSizeEditor) {", "        QFont f;", "        f.setFamilies(codeFamilies());", "        f.setStyleHint(QFont::Monospace);", "        f.setFixedPitch(true);", "        f.setPixelSize(pixelSize);", "        return f;", "    }", "", "    inline QFont uiFont(int pixelSize = FontSizeUI) {", "        QFont f;", "        f.setFamilies(uiFamilies());", "        f.setStyleHint(QFont::SansSerif);", "        f.setPixelSize(pixelSize);", "        return f;", "    }", "", "    // A CSS family list for the stylesheets that cannot take a QFont.", "    inline QString codeFamilyCss() {", "        return QStringLiteral(\"'JetBrains Mono','Cascadia Code','Cascadia Mono','Consolas',monospace\");", "    }", "    inline QString uiFamilyCss() {", "        return QStringLiteral(\"'Segoe UI Variable Text','Segoe UI','Inter',system-ui,sans-serif\");", "    }", "", "    // -- Icons --", "    //", "    // Windows ships a real icon font. Using it beats scattering Unicode glyphs", "    // like - and - through the UI, which land in whatever fallback face happens", "    // to carry them and end up at different weights and baselines from each", "    // other - one of the clearest signs of an improvised interface.", "    inline QFont iconFont(int pixelSize = 12) {", "        QFont f;", "        f.setFamilies({QStringLiteral(\"Segoe Fluent Icons\"),    // Windows 11", "                       QStringLiteral(\"Segoe MDL2 Assets\")});   // Windows 10", "        f.setPixelSize(pixelSize);", "        return f;", "    }", "", "    namespace Icon {", "        inline QString glyph(char16_t c) { return QString(QChar(c)); }", "        inline const QString Add       = glyph(0xE710);", "        inline const QString NewFolder = glyph(0xE8F4);", "        inline const QString Refresh   = glyph(0xE72C);", "        inline const QString Collapse  = glyph(0xE70E);   // chevron up", "        inline const QString Close     = glyph(0xE711);", "        inline const QString Play      = glyph(0xE768);", "        inline const QString Stop      = glyph(0xE71A);", "        inline const QString Delete    = glyph(0xE74D);   // waste basket", "        inline const QString Clear     = glyph(0xE894);", "        inline const QString Folder    = glyph(0xE8B7);", "    }", "", "    // -- Named roles --", "    // Call sites ask for the role, not the face, so the mapping above is the", "    // only place that ever has to change.", "    inline QFont editorFont()   { return codeFont(FontSizeEditor); }     // the code surface", "    inline QFont terminalFont() { return codeFont(FontSizeTerminal); }   // shell + test I/O", "    inline QFont sidebarFont()  { return uiFont(FontSizeUI); }           // explorer, tabs", "    inline QFont statusFont()   { return uiFont(FontSizeSmall); }        // status bar, headers", "}", "#pragma once", "#include <utility>", "", "struct Position {", "    int row = 0;", "    int col = 0;", "", "    bool operator==(const Position& o) const { return row == o.row && col == o.col; }", "    bool operator!=(const Position& o) const { return !(*this == o); }", "    bool operator<(const Position& o) const {", "        return row < o.row || (row == o.row && col < o.col);", "    }", "    bool operator>(const Position& o) const { return o < *this; }", "    bool operator<=(const Position& o) const { return !(o < *this); }", "    bool operator>=(const Position& o) const { return !(*this < o); }", "};", "", "struct Selection {", "    Position anchor;", "    bool active = false;", "", "    void start(Position pos) { anchor = pos; active = true; }", "    void clear() { active = false; }", "", "    bool hasSelection(const Position& cursor) const {", "        return active && anchor != cursor;", "    }", "", "    std::pair<Position, Position> normalized(const Position& cursor) const {", "        if (anchor < cursor) return {anchor, cursor};", "        return {cursor, anchor};", "    }", "};", "#pragma once", "#include <vector>", "#include <string>", "#include \"selection.h\"", "", "class TextBuffer {", "public:", "    TextBuffer();", "", "    // Single-char operations", "    void insertChar(int row, int col, char ch);", "    void deleteChar(int row, int col);", "    void splitLine(int row, int col);", "    void mergeLines(int row);", "", "    // Range operations", "    std::string getText(Position start, Position end) const;", "    void deleteRange(Position start, Position end);", "    Position insertText(int row, int col, const std::string& text);", "", "    // Word operations", "    int findWordBoundaryLeft(int row, int col) const;", "    int findWordBoundaryRight(int row, int col) const;", "", "    // Auto-indent", "    std::string getLeadingWhitespace(int row) const;", "", "    // File I/O", "    bool loadFromFile(const std::string& path);", "    bool saveToFile(const std::string& path) const;", "", "    // Accessors", "    int lineCount() const;", "    int lineLength(int row) const;", "    const std::string& line(int row) const;", "", "    void clear();", "", "private:", "    std::vector<std::string> lines_;", "};", "#include \"text_buffer.h\"", "#include <fstream>", "#include <sstream>", "#include <algorithm>", "#include <cctype>", "", "TextBuffer::TextBuffer() {", "    lines_.push_back(\"\");", "}", "", "void TextBuffer::insertChar(int row, int col, char ch) {", "    if (row < 0 || row >= (int)lines_.size()) return;", "    col = std::clamp(col, 0, (int)lines_[row].size());", "    lines_[row].insert(col, 1, ch);", "}", "", "void TextBuffer::deleteChar(int row, int col) {", "    if (row < 0 || row >= (int)lines_.size()) return;", "    if (col <= 0) {", "        mergeLines(row);", "        return;", "    }", "    if (col <= (int)lines_[row].size()) {", "        lines_[row].erase(col - 1, 1);", "    }", "}", "", "void TextBuffer::splitLine(int row, int col) {", "    if (row < 0 || row >= (int)lines_.size()) return;", "    col = std::clamp(col, 0, (int)lines_[row].size());", "    std::string tail = lines_[row].substr(col);", "    lines_[row] = lines_[row].substr(0, col);", "    lines_.insert(lines_.begin() + row + 1, tail);", "}", "", "void TextBuffer::mergeLines(int row) {", "    if (row <= 0 || row >= (int)lines_.size()) return;", "    lines_[row - 1] += lines_[row];", "    lines_.erase(lines_.begin() + row);", "}", "", "std::string TextBuffer::getText(Position start, Position end) const {", "    if (start > end) std::swap(start, end);", "    if (start.row == end.row) {", "        int s = std::clamp(start.col, 0, (int)lines_[start.row].size());", "        int e = std::clamp(end.col, 0, (int)lines_[start.row].size());", "        return lines_[start.row].substr(s, e - s);", "    }", "", "    std::string result;", "    // First line", "    result += lines_[start.row].substr(start.col);", "    result += '\\n';", "    // Middle lines", "    for (int r = start.row + 1; r < end.row; r++) {", "        result += lines_[r];", "        result += '\\n';", "    }", "    // Last line", "    int e = std::clamp(end.col, 0, (int)lines_[end.row].size());", "    result += lines_[end.row].substr(0, e);", "    return result;", "}", "", "void TextBuffer::deleteRange(Position start, Position end) {", "    if (start > end) std::swap(start, end);", "    if (start.row == end.row) {", "        int s = std::clamp(start.col, 0, (int)lines_[start.row].size());", "        int e = std::clamp(end.col, 0, (int)lines_[start.row].size());", "        lines_[start.row].erase(s, e - s);", "        return;", "    }", "", "    // Keep prefix of first line + suffix of last line", "    std::string prefix = lines_[start.row].substr(0, start.col);", "    int ec = std::clamp(end.col, 0, (int)lines_[end.row].size());", "    std::string suffix = lines_[end.row].substr(ec);", "", "    // Erase lines from start.row to end.row (inclusive)", "    lines_.erase(lines_.begin() + start.row, lines_.begin() + end.row + 1);", "    lines_.insert(lines_.begin() + start.row, prefix + suffix);", "}", "", "Position TextBuffer::insertText(int row, int col, const std::string& text) {", "    if (text.empty()) return {row, col};", "", "    // Split the text into lines", "    std::vector<std::string> newLines;", "    std::istringstream stream(text);", "    std::string line;", "    while (std::getline(stream, line)) {", "        // Remove \\r if present", "        if (!line.empty() && line.back() == '\\r') line.pop_back();", "        newLines.push_back(line);", "    }", "    // If text ends with newline, add empty line", "    if (!text.empty() && text.back() == '\\n') {", "        newLines.push_back(\"\");", "    }", "    if (newLines.empty()) return {row, col};", "", "    col = std::clamp(col, 0, (int)lines_[row].size());", "    std::string prefix = lines_[row].substr(0, col);", "    std::string suffix = lines_[row].substr(col);", "", "    if (newLines.size() == 1) {", "        lines_[row] = prefix + newLines[0] + suffix;", "        return {row, (int)(prefix.size() + newLines[0].size())};", "    }", "", "    // Multi-line insert", "    lines_[row] = prefix + newLines[0];", "    for (int i = 1; i < (int)newLines.size() - 1; i++) {", "        lines_.insert(lines_.begin() + row + i, newLines[i]);", "    }", "    int lastIdx = row + (int)newLines.size() - 1;", "    lines_.insert(lines_.begin() + lastIdx, newLines.back() + suffix);", "    return {lastIdx, (int)newLines.back().size()};", "}", "", "int TextBuffer::findWordBoundaryLeft(int row, int col) const {", "    if (col <= 0) return 0;", "    const std::string& l = lines_[row];", "    int i = col - 1;", "    // Skip whitespace", "    while (i > 0 && std::isspace((unsigned char)l[i])) i--;", "    // Skip word chars", "    if (i >= 0 && std::isalnum((unsigned char)l[i])) {", "        while (i > 0 && std::isalnum((unsigned char)l[i - 1])) i--;", "    } else if (i >= 0) {", "        // Skip punctuation", "        while (i > 0 && !std::isalnum((unsigned char)l[i - 1]) && !std::isspace((unsigned char)l[i - 1])) i--;", "    }", "    return i;", "}", "", "int TextBuffer::findWordBoundaryRight(int row, int col) const {", "    const std::string& l = lines_[row];", "    int len = (int)l.size();", "    if (col >= len) return len;", "    int i = col;", "    // Skip current word chars", "    if (std::isalnum((unsigned char)l[i])) {", "        while (i < len && std::isalnum((unsigned char)l[i])) i++;", "    } else if (!std::isspace((unsigned char)l[i])) {", "        while (i < len && !std::isalnum((unsigned char)l[i]) && !std::isspace((unsigned char)l[i])) i++;", "    }", "    // Skip whitespace", "    while (i < len && std::isspace((unsigned char)l[i])) i++;", "    return i;", "}", "", "std::string TextBuffer::getLeadingWhitespace(int row) const {", "    if (row < 0 || row >= (int)lines_.size()) return \"\";", "    const std::string& l = lines_[row];", "    size_t i = 0;", "    while (i < l.size() && (l[i] == ' ' || l[i] == '\\t')) i++;", "    return l.substr(0, i);", "}", "", "bool TextBuffer::loadFromFile(const std::string& path) {", "    std::ifstream file(path);", "    if (!file.is_open()) return false;", "", "    lines_.clear();", "    lines_.reserve(1024);            // a large file should not realloc dozens of times", "", "    std::string line;", "    while (std::getline(file, line)) {", "        // Text mode already folds CRLF on Windows, but a file with CR endings", "        // read on another platform would otherwise leave a stray CR at the end", "        // of every line, offsetting every column by one.", "        if (!line.empty() && line.back() == '\\r') line.pop_back();", "        lines_.push_back(line);", "    }", "    if (lines_.empty()) lines_.push_back(\"\");", "    return true;", "}", "", "bool TextBuffer::saveToFile(const std::string& path) const {", "    std::ofstream file(path);", "    if (!file.is_open()) return false;", "", "    for (int i = 0; i < (int)lines_.size(); i++) {", "        file << lines_[i];", "        // Terminate every line, the last one included. Without the final", "        // newline, opening a normal source file and saving it unchanged", "        // silently stripped the trailing newline it arrived with.", "        file << '\\n';", "    }", "", "    // is_open() only says the file was created. A full disk or a read-only", "    // target fails here, and reporting success would tell the user their work", "    // was saved when it was not.", "    file.flush();", "    if (!file.good()) return false;", "    file.close();", "    return file.good();", "}", "", "int TextBuffer::lineCount() const { return (int)lines_.size(); }", "int TextBuffer::lineLength(int row) const {", "    if (row < 0 || row >= (int)lines_.size()) return 0;", "    return (int)lines_[row].size();", "}", "const std::string& TextBuffer::line(int row) const {", "    // Row indices reach here from mouse coordinates and scroll arithmetic, so", "    // an out-of-range value is a rendering glitch rather than a crash.", "    static const std::string kEmpty;", "    if (row < 0 || row >= (int)lines_.size()) return kEmpty;", "    return lines_[row];", "}", "", "void TextBuffer::clear() {", "    lines_.clear();", "    lines_.push_back(\"\");", "}", "#pragma once", "#include <vector>", "#include <string>", "#include <chrono>", "#include \"selection.h\"", "", "struct EditAction {", "    enum Type { Insert, Delete };", "    Type type;", "    Position pos;", "    std::string text;", "    std::chrono::steady_clock::time_point timestamp;", "};", "", "class UndoManager {", "public:", "    UndoManager();", "", "    void recordInsert(Position pos, const std::string& text);", "    void recordDelete(Position pos, const std::string& text);", "    void forceNewGroup();", "", "    // Forces every edit recorded between the two calls into a single undo step,", "    // regardless of shape or timing. Needed because the automatic grouping only", "    // ever merges single-character edits, so a compound operation such as", "    // indenting a block would otherwise take one Ctrl+Z per line.", "    void beginCompound();", "    void endCompound();", "", "    struct UndoResult {", "        bool valid = false;", "        std::vector<EditAction> actions;", "    };", "", "    UndoResult undo();", "    UndoResult redo();", "", "    bool canUndo() const;", "    bool canRedo() const;", "    void clear();", "", "private:", "    using ActionGroup = std::vector<EditAction>;", "    std::vector<ActionGroup> undoStack_;", "    std::vector<ActionGroup> redoStack_;", "    bool forceNext_ = false;", "    int compoundDepth_ = 0;", "    bool compoundStarted_ = false;", "", "    bool shouldGroup(const EditAction& action) const;", "    void push(const EditAction& action);", "    static constexpr int GROUP_TIMEOUT_MS = 400;", "    // Undo history is otherwise unbounded: a long editing session grows it", "    // forever. Old groups are dropped from the bottom.", "    static constexpr size_t MAX_GROUPS = 4000;", "    void trim();", "};", "#include \"undo_manager.h\"", "", "UndoManager::UndoManager() {}", "", "bool UndoManager::shouldGroup(const EditAction& action) const {", "    if (forceNext_) return false;", "    if (undoStack_.empty() || undoStack_.back().empty()) return false;", "", "    const auto& lastGroup = undoStack_.back();", "    const auto& last = lastGroup.back();", "", "    // Only group single-char inserts with single-char inserts (and same for deletes)", "    if (last.type != action.type) return false;", "    if (last.text.size() != 1 || action.text.size() != 1) return false;", "", "    // Don't group across whitespace boundaries", "    bool lastIsSpace = (last.text[0] == ' ' || last.text[0] == '\\t');", "    bool curIsSpace = (action.text[0] == ' ' || action.text[0] == '\\t');", "    if (lastIsSpace != curIsSpace) return false;", "", "    // Check time", "    auto elapsed = std::chrono::duration_cast<std::chrono::milliseconds>(", "        action.timestamp - last.timestamp).count();", "    if (elapsed > GROUP_TIMEOUT_MS) return false;", "", "    // Check adjacency", "    if (action.type == EditAction::Insert) {", "        // Cursor should be right after the last insert", "        if (action.pos.row != last.pos.row) return false;", "        if (action.pos.col != last.pos.col + (int)last.text.size()) return false;", "    } else {", "        // For deletes, cursor should be at or near the last delete", "        if (action.pos.row != last.pos.row) return false;", "    }", "", "    return true;", "}", "", "void UndoManager::recordInsert(Position pos, const std::string& text) {", "    EditAction action{EditAction::Insert, pos, text,", "                      std::chrono::steady_clock::now()};", "", "    redoStack_.clear();", "    push(action);", "}", "", "void UndoManager::recordDelete(Position pos, const std::string& text) {", "    EditAction action{EditAction::Delete, pos, text,", "                      std::chrono::steady_clock::now()};", "", "    redoStack_.clear();", "    push(action);", "}", "", "// Everything recorded inside a compound joins the group the compound opened.", "void UndoManager::push(const EditAction& action) {", "    if (compoundDepth_ > 0 && compoundStarted_ && !undoStack_.empty()) {", "        undoStack_.back().push_back(action);", "    } else if (shouldGroup(action)) {", "        undoStack_.back().push_back(action);", "    } else {", "        undoStack_.push_back({action});", "        trim();", "    }", "    if (compoundDepth_ > 0) compoundStarted_ = true;", "    forceNext_ = false;", "}", "", "void UndoManager::beginCompound() {", "    if (compoundDepth_++ == 0) {", "        forceNext_ = true;        // the first edit inside opens a fresh group", "        compoundStarted_ = false;", "    }", "}", "", "void UndoManager::endCompound() {", "    if (compoundDepth_ > 0 && --compoundDepth_ == 0) {", "        compoundStarted_ = false;", "        forceNext_ = true;        // the next edit must not join the compound", "    }", "}", "", "void UndoManager::trim() {", "    if (undoStack_.size() > MAX_GROUPS) {", "        undoStack_.erase(undoStack_.begin(),", "                         undoStack_.begin() + (undoStack_.size() - MAX_GROUPS));", "    }", "}", "", "void UndoManager::forceNewGroup() {", "    forceNext_ = true;", "}", "", "UndoManager::UndoResult UndoManager::undo() {", "    if (undoStack_.empty()) return {false, {}};", "", "    auto group = undoStack_.back();", "    undoStack_.pop_back();", "    redoStack_.push_back(group);", "", "    // Reverse the actions for undo - process in reverse order", "    UndoResult result;", "    result.valid = true;", "    result.actions.assign(group.rbegin(), group.rend());", "    return result;", "}", "", "UndoManager::UndoResult UndoManager::redo() {", "    if (redoStack_.empty()) return {false, {}};", "", "    auto group = redoStack_.back();", "    redoStack_.pop_back();", "    undoStack_.push_back(group);", "", "    UndoResult result;", "    result.valid = true;", "    result.actions = group; // forward order", "    return result;", "}", "", "bool UndoManager::canUndo() const { return !undoStack_.empty(); }", "bool UndoManager::canRedo() const { return !redoStack_.empty(); }", "", "void UndoManager::clear() {", "    undoStack_.clear();", "    redoStack_.clear();", "    forceNext_ = false;", "}", "#pragma once", "#include <vector>", "#include <string>", "#include <string_view>", "#include <set>", "", "enum class TokenType {", "    Plain, Keyword, Type, String, Comment, Number,", "    Preprocessor, Function, Punctuation", "};", "", "struct Token {", "    TokenType type;", "    int start;", "    int length;", "};", "", "class CppHighlighter {", "public:", "    CppHighlighter();", "    std::vector<Token> tokenize(const std::string& line, bool& inBlockComment) const;", "", "private:", "    // Transparent comparators, so a std::string_view can be looked up directly.", "    // tokenize() runs for every visible line on every repaint; building a", "    // std::string per identifier just to probe a set is pure waste.", "    std::set<std::string, std::less<>> keywords_;", "    std::set<std::string, std::less<>> types_;", "", "    static bool isIdentStart(char c);", "    static bool isIdentChar(char c);", "};", "#include \"cpp_highlighter.h\"", "#include <cctype>", "", "CppHighlighter::CppHighlighter() {", "    keywords_ = {", "        \"if\", \"else\", \"for\", \"while\", \"do\", \"switch\", \"case\", \"break\",", "        \"continue\", \"return\", \"class\", \"struct\", \"enum\", \"namespace\",", "        \"template\", \"typename\", \"public\", \"private\", \"protected\",", "        \"virtual\", \"override\", \"final\", \"const\", \"constexpr\", \"static\",", "        \"inline\", \"extern\", \"volatile\", \"mutable\", \"explicit\",", "        \"void\", \"new\", \"delete\", \"try\", \"catch\", \"throw\", \"noexcept\",", "        \"using\", \"typedef\", \"auto\", \"nullptr\", \"true\", \"false\",", "        \"this\", \"operator\", \"sizeof\", \"alignof\", \"decltype\",", "        \"static_cast\", \"dynamic_cast\", \"reinterpret_cast\", \"const_cast\",", "        \"include\", \"define\", \"pragma\", \"ifdef\", \"ifndef\", \"endif\", \"elif\",", "        \"default\", \"goto\", \"register\", \"friend\", \"concept\", \"requires\",", "        \"co_await\", \"co_return\", \"co_yield\", \"consteval\", \"constinit\"", "    };", "", "    types_ = {", "        \"int\", \"char\", \"float\", \"double\", \"bool\", \"long\", \"short\",", "        \"unsigned\", \"signed\", \"wchar_t\", \"char8_t\", \"char16_t\", \"char32_t\",", "        \"string\", \"vector\", \"map\", \"unordered_map\", \"set\", \"unordered_set\",", "        \"array\", \"list\", \"deque\", \"stack\", \"queue\", \"pair\", \"tuple\",", "        \"shared_ptr\", \"unique_ptr\", \"weak_ptr\", \"optional\", \"variant\",", "        \"size_t\", \"ptrdiff_t\", \"int8_t\", \"int16_t\", \"int32_t\", \"int64_t\",", "        \"uint8_t\", \"uint16_t\", \"uint32_t\", \"uint64_t\",", "        \"QString\", \"QWidget\", \"QMainWindow\", \"QApplication\",", "        \"QColor\", \"QFont\", \"QPainter\", \"QTimer\", \"QEvent\",", "        \"std\", \"cout\", \"cin\", \"endl\", \"cerr\",", "        \"ifstream\", \"ofstream\", \"fstream\", \"stringstream\", \"istringstream\", \"ostringstream\"", "    };", "}", "", "bool CppHighlighter::isIdentStart(char c) {", "    return std::isalpha((unsigned char)c) || c == '_';", "}", "", "bool CppHighlighter::isIdentChar(char c) {", "    return std::isalnum((unsigned char)c) || c == '_';", "}", "", "std::vector<Token> CppHighlighter::tokenize(const std::string& line, bool& inBlockComment) const {", "    std::vector<Token> tokens;", "    int i = 0;", "    int len = (int)line.size();", "    tokens.reserve(line.size() / 4 + 8);", "", "    while (i < len) {", "        // Inside block comment continuation", "        if (inBlockComment) {", "            int start = i;", "            while (i < len) {", "                if (i + 1 < len && line[i] == '*' && line[i + 1] == '/') {", "                    i += 2;", "                    inBlockComment = false;", "                    break;", "                }", "                i++;", "            }", "            tokens.push_back({TokenType::Comment, start, i - start});", "            continue;", "        }", "", "        char c = line[i];", "", "        // Skip whitespace (emit as plain)", "        if (std::isspace((unsigned char)c)) {", "            int start = i;", "            while (i < len && std::isspace((unsigned char)line[i])) i++;", "            tokens.push_back({TokenType::Plain, start, i - start});", "            continue;", "        }", "", "        // Line comment", "        if (c == '/' && i + 1 < len && line[i + 1] == '/') {", "            tokens.push_back({TokenType::Comment, i, len - i});", "            i = len;", "            continue;", "        }", "", "        // Block comment start", "        if (c == '/' && i + 1 < len && line[i + 1] == '*') {", "            int start = i;", "            i += 2;", "            inBlockComment = true;", "            while (i < len) {", "                if (i + 1 < len && line[i] == '*' && line[i + 1] == '/') {", "                    i += 2;", "                    inBlockComment = false;", "                    break;", "                }", "                i++;", "            }", "            tokens.push_back({TokenType::Comment, start, i - start});", "            continue;", "        }", "", "        // Preprocessor", "        if (c == '#') {", "            tokens.push_back({TokenType::Preprocessor, i, len - i});", "            i = len;", "            continue;", "        }", "", "        // String literal", "        if (c == '\"' || c == '\\'') {", "            char quote = c;", "            int start = i;", "            i++;", "            while (i < len && line[i] != quote) {", "                if (line[i] == '\\\\' && i + 1 < len) i++; // skip escape", "                i++;", "            }", "            if (i < len) i++; // closing quote", "            tokens.push_back({TokenType::String, start, i - start});", "            continue;", "        }", "", "        // Number", "        if (std::isdigit((unsigned char)c) ||", "            (c == '.' && i + 1 < len && std::isdigit((unsigned char)line[i + 1]))) {", "            int start = i;", "            auto isSeparator = [&](int at) {", "                // 1'000'000'007 is ordinary competitive-programming code. Without", "                // this the apostrophe opened a character literal and painted the", "                // rest of the line as a string.", "                return line[at] == '\\'' && at + 1 < len &&", "                       std::isalnum((unsigned char)line[at + 1]);", "            };", "", "            if (c == '0' && i + 1 < len && (line[i + 1] == 'x' || line[i + 1] == 'X')) {", "                i += 2;", "                while (i < len && (std::isxdigit((unsigned char)line[i]) || isSeparator(i))) i++;", "            } else if (c == '0' && i + 1 < len && (line[i + 1] == 'b' || line[i + 1] == 'B')) {", "                i += 2;", "                while (i < len && (line[i] == '0' || line[i] == '1' || isSeparator(i))) i++;", "            } else {", "                while (i < len && (std::isdigit((unsigned char)line[i]) ||", "                                   line[i] == '.' || isSeparator(i))) i++;", "                if (i < len && (line[i] == 'e' || line[i] == 'E')) {", "                    i++;", "                    if (i < len && (line[i] == '+' || line[i] == '-')) i++;", "                    while (i < len && std::isdigit((unsigned char)line[i])) i++;", "                }", "            }", "            // Suffixes like f, L, u, ll", "            while (i < len && (line[i] == 'f' || line[i] == 'F' || line[i] == 'l' ||", "                               line[i] == 'L' || line[i] == 'u' || line[i] == 'U')) i++;", "            tokens.push_back({TokenType::Number, start, i - start});", "            continue;", "        }", "", "        // Identifier / keyword / type / function", "        if (isIdentStart(c)) {", "            int start = i;", "            while (i < len && isIdentChar(line[i])) i++;", "            const std::string_view word(line.data() + start, i - start);", "", "            // Check if followed by '(' - function", "            int peek = i;", "            while (peek < len && std::isspace((unsigned char)line[peek])) peek++;", "            bool isFunc = (peek < len && line[peek] == '(');", "", "            if (keywords_.count(word)) {", "                tokens.push_back({TokenType::Keyword, start, i - start});", "            } else if (types_.count(word)) {", "                tokens.push_back({TokenType::Type, start, i - start});", "            } else if (isFunc) {", "                tokens.push_back({TokenType::Function, start, i - start});", "            } else {", "                tokens.push_back({TokenType::Plain, start, i - start});", "            }", "            continue;", "        }", "", "        // Punctuation (operators, braces, etc.). Emitted as a run rather than one", "        // token per character: \"}));\" is one draw call, not four.", "        {", "            int start = i;", "            while (i < len) {", "                const char p = line[i];", "                if (std::isspace((unsigned char)p) || isIdentStart(p) ||", "                    std::isdigit((unsigned char)p) || p == '\"' || p == '\\'' || p == '#') break;", "                // Stop before something that starts a comment.", "                if (p == '/' && i + 1 < len && (line[i + 1] == '/' || line[i + 1] == '*')) break;", "                i++;", "            }", "            if (i == start) i++;        // always make progress", "            tokens.push_back({TokenType::Punctuation, start, i - start});", "        }", "    }", "", "    return tokens;", "}", "#pragma once", "#include <QString>", "#include <QVector>", "", "// A code template offered as you type its trigger.", "//", "// `body` carries a single CARET marker saying where the caret lands after the", "// expansion. Using a sentinel rather than a row/column pair means the template", "// can be edited freely without anyone recounting lines.", "struct Snippet {", "    QString trigger;   // what you type", "    QString detail;    // the one-line description shown beside it", "    QString body;      // insertion text, containing exactly one CARET", "};", "", "namespace Snippets {", "", "// U+0001 - cannot occur in source the user typed, so it is unambiguous.", "inline const QChar Caret = QChar(0x0001);", "", "inline const QVector<Snippet>& all() {", "    static const QVector<Snippet> table = {", "        {", "            QStringLiteral(\"cppmain\"),", "            QStringLiteral(\"C++ template\"),", "            QStringLiteral(\"#include <bits/stdc++.h>\\n\"", "                           \"using namespace std;\\n\"", "                           \"\\n\"", "                           \"int main() {\\n\"", "                           \"    \\x01\\n\"", "                           \"    return 0;\\n\"", "                           \"}\")", "        },", "    };", "    return table;", "}", "", "// Typing fewer characters than this never opens the suggestion list, so normal", "// typing is not interrupted by a popup after one or two letters.", "inline constexpr int MinPrefix = 3;", "", "inline QVector<Snippet> matching(const QString& prefix) {", "    QVector<Snippet> hits;", "    if (prefix.size() < MinPrefix) return hits;", "    for (const Snippet& s : all()) {", "        // Case-insensitive so \"CPP\" finds it, but the trigger still has to be a", "        // genuine prefix - this is a trigger list, not a fuzzy search.", "        if (s.trigger.startsWith(prefix, Qt::CaseInsensitive)) hits.append(s);", "    }", "    return hits;", "}", "", "} // namespace Snippets", "#pragma once", "#include <QWidget>", "#include <QFont>", "#include <QTimer>", "#include <QRect>", "#include <QVariantAnimation>", "#include \"../core/text_buffer.h\"", "#include \"../core/undo_manager.h\"", "#include \"../core/selection.h\"", "#include \"cpp_highlighter.h\"", "#include \"snippets.h\"", "#include \"../theme/theme.h\"", "", "class SnippetPopup;", "", "class EditorWidget : public QWidget {", "    Q_OBJECT", "", "public:", "    explicit EditorWidget(QWidget* parent = nullptr);", "", "    bool openFile(const QString& path);", "    bool saveFile();", "    bool saveFileAs(const QString& path);", "", "    QString filePath() const;", "    // Follows an on-disk rename without touching the buffer or the", "    // modified flag - the content did not change, only its name.", "    void setFilePath(const QString& path);", "    bool isModified() const;", "    QString fileName() const;", "", "    int currentRow() const;", "    int currentCol() const;", "    int totalLines() const;", "", "    // For external line number area queries", "    int firstVisibleRow() const;", "    int lastVisibleRow() const;", "    int getGutterWidth() const;", "    int getCharHeight() const;", "    int getScrollY() const;", "    int getCharWidth() const;", "", "    // Public editing operations (called by menus)", "    void performUndo();", "    void performRedo();", "    void copy();", "    void cut();", "    void paste();", "    void selectAll();", "    void setSelection(int startRow, int startCol, int endRow, int endCol);", "", "    // -- Zoom --", "    // Sized in pixels so the value means the same thing on every display.", "    void setFontPixelSize(int px);", "    int  fontPixelSize() const { return fontPixelSize_; }", "    static constexpr int MinFontPx = 8;", "    static constexpr int MaxFontPx = 40;", "", "signals:", "    void modifiedChanged(bool modified);", "    // Ctrl+wheel asks the window to zoom every editor, not just this one.", "    void zoomStepRequested(int steps);", "    void cursorPositionChanged(int row, int col);", "    void saveRequested();", "", "protected:", "    bool event(QEvent* e) override;", "    void paintEvent(QPaintEvent* event) override;", "    void keyPressEvent(QKeyEvent* event) override;", "    void mousePressEvent(QMouseEvent* event) override;", "    void mouseDoubleClickEvent(QMouseEvent* event) override;", "    void mouseMoveEvent(QMouseEvent* event) override;", "    void wheelEvent(QWheelEvent* event) override;", "    void resizeEvent(QResizeEvent* event) override;", "", "private slots:", "    void toggleCursorBlink();", "", "private:", "    // Core data", "    TextBuffer buffer_;", "    UndoManager undoManager_;", "    Position cursor_;", "    Selection selection_;", "    CppHighlighter highlighter_;", "", "    // Block comment state at the START of each line. Maintained incrementally:", "    // a full rescan on every keystroke is O(file) and shows up as lag well", "    // before a file gets big.", "    std::vector<bool> blockCommentState_;", "    void rebuildCommentState(int fromRow);", "    void rebuildCommentStateFull();", "", "    // Rendering", "    QFont font_;", "    int charWidth_;", "    int charHeight_;", "    int ascent_;", "    int scrollY_;", "    int scrollX_ = 0;", "    int gutterWidth_;", "    int gutterPadding_;", "    // True when the font really is fixed-pitch, which lets a whole token be", "    // drawn in one call instead of a character at a time.", "    bool monospaceExact_ = false;", "", "    // Cursor", "    QTimer* blinkTimer_;", "    bool cursorVisible_;", "", "    // Smooth scrolling. scrollTarget_ is where the view is heading, so a second", "    // wheel tick mid-flight retargets rather than restarting from where the", "    // animation happens to be.", "    QVariantAnimation* scrollAnim_ = nullptr;", "    int scrollTarget_ = 0;", "", "    // Zoom", "    int fontPixelSize_ = Theme::FontSizeEditor;", "    void applyFontMetrics();", "", "    // Snippets", "    SnippetPopup* snippetPopup_ = nullptr;", "    QString wordBeforeCursor(int* startCol = nullptr) const;", "    void updateSnippetSuggestion();", "    void hideSnippetSuggestion();", "    bool acceptSnippet();", "", "    // File", "    QString filePath_;", "    bool modified_;", "", "    // Helpers", "    void updateGutterWidth();", "    void ensureCursorVisible();", "    void setModified(bool m);", "    void resetCursorBlink();", "", "    // Cursor movement", "    void moveCursorLeft(bool shift);", "    void moveCursorRight(bool shift);", "    void moveCursorUp(bool shift);", "    void moveCursorDown(bool shift);", "    void moveCursorHome(bool shift, bool ctrl);", "    void moveCursorEnd(bool shift, bool ctrl);", "    void moveCursorWordLeft(bool shift);", "    void moveCursorWordRight(bool shift);", "", "    // Selection helpers", "    void updateSelectionForMove(bool shift);", "    void deleteSelection();", "    std::string getSelectedText() const;", "", "    // Edit operations", "    void handleBackspace(bool ctrl);", "    void handleDelete();", "    void handleEnter();", "    void handleTab(bool shift);", "    void handleChar(char ch);", "    void movePage(int direction, bool shift);", "", "    // -- Indentation & bracket intelligence --", "    static constexpr int INDENT_WIDTH = 4;", "    static char closerFor(char open);", "    static bool isCloser(char c);", "    static bool isIdentLike(char c);", "", "    int indentWidthOf(const std::string& line) const;", "    bool onlyWhitespaceBefore(int row, int col) const;", "    // Walks back to the '{' this '}' closes, skipping braces that live inside", "    // strings and comments. Returns false if the block is unbalanced.", "    bool findMatchingOpenBrace(Position closePos, Position& out) const;", "    void reindentClosingBrace();", "    void indentBlock(int firstRow, int lastRow, bool unindent);", "    void adjustColAfterIndent(int row, int delta);", "", "    // (clipboard & undo/redo are public - see above)", "", "    // Coordinate conversion", "    int rowFromY(int y) const;", "    int colFromX(int x, int row) const;", "    int xFromCol(int col) const;", "    int yFromRow(int row) const;", "", "    // Scroll", "    int maxScrollY() const;", "    int maxScrollX() const;", "    void clampScroll();", "    void animateScrollTo(int targetY);", "    void jumpScrollTo(int y);              // instant; keeps the animation in sync", "    QRect cursorRect() const;", "    int codeLeft() const { return gutterWidth_; }", "", "    // Paint helpers", "    void paintGutter(QPainter& p, int startRow, int endRow);", "    void paintCode(QPainter& p, int startRow, int endRow);", "    void paintCursor(QPainter& p);", "    void paintSelection(QPainter& p, int row, int y);", "};", "#include \"editor_widget.h\"", "#include \"snippet_popup.h\"", "#include \"../theme/theme.h\"", "#include <QPainter>", "#include <QKeyEvent>", "#include <QMouseEvent>", "#include <QWheelEvent>", "#include <QFontMetrics>", "#include <QClipboard>", "#include <QApplication>", "#include <algorithm>", "#include <cmath>", "", "void EditorWidget::applyFontMetrics() {", "    font_ = Theme::codeFont(fontPixelSize_);", "    QFontMetrics fm(font_);", "    charWidth_ = qMax(1, fm.horizontalAdvance(QLatin1Char('M')));", "    charHeight_ = fm.height();", "    ascent_ = fm.ascent();", "", "    // Verify the font really is fixed-pitch before trusting a whole-token draw.", "    // If a fallback font sneaks in, glyph advances stop matching the grid and", "    // the caret drifts away from the text - the bug the per-character path was", "    // originally written to fix. Re-checked on every zoom, because hinting at a", "    // small size can round advances differently.", "    monospaceExact_ =", "        fm.horizontalAdvance(QStringLiteral(\"MMMMMMMMMM\")) == 10 * charWidth_ &&", "        fm.horizontalAdvance(QStringLiteral(\"iiiiiiiiii\")) == 10 * charWidth_;", "", "    updateGutterWidth();", "}", "", "void EditorWidget::setFontPixelSize(int px) {", "    px = std::clamp(px, MinFontPx, MaxFontPx);", "    if (px == fontPixelSize_) return;", "    fontPixelSize_ = px;", "", "    applyFontMetrics();", "    // Keep the caret where it was on screen rather than letting the view jump", "    // to an unrelated part of the file as the line height changes.", "    jumpScrollTo(cursor_.row * charHeight_ - height() / 2);", "    ensureCursorVisible();", "    hideSnippetSuggestion();", "    update();", "}", "", "EditorWidget::EditorWidget(QWidget* parent) : QWidget(parent) {", "    applyFontMetrics();", "", "    scrollY_ = 0;", "    gutterPadding_ = 14;", "    updateGutterWidth();", "", "    modified_ = false;", "    cursorVisible_ = true;", "", "    blinkTimer_ = new QTimer(this);", "    connect(blinkTimer_, &QTimer::timeout, this, &EditorWidget::toggleCursorBlink);", "    blinkTimer_->start(500);", "", "    scrollAnim_ = new QVariantAnimation(this);", "    scrollAnim_->setDuration(140);", "    scrollAnim_->setEasingCurve(QEasingCurve::OutCubic);", "    connect(scrollAnim_, &QVariantAnimation::valueChanged, this,", "            [this](const QVariant& v) {", "        scrollY_ = v.toInt();", "        clampScroll();", "        update();", "    });", "", "    snippetPopup_ = new SnippetPopup(this);", "", "    setFocusPolicy(Qt::StrongFocus);", "    setMouseTracking(true);", "    setCursor(Qt::IBeamCursor);", "", "    rebuildCommentStateFull();", "}", "", "// -- File Operations --", "", "bool EditorWidget::openFile(const QString& path) {", "    if (!buffer_.loadFromFile(path.toStdString())) return false;", "    filePath_ = path;", "    cursor_ = {0, 0};", "    scrollY_ = 0;", "    selection_.clear();", "    undoManager_.clear();", "    setModified(false);", "    updateGutterWidth();", "    rebuildCommentStateFull();", "    update();", "    return true;", "}", "", "bool EditorWidget::saveFile() {", "    if (filePath_.isEmpty()) return false;", "    return saveFileAs(filePath_);", "}", "", "bool EditorWidget::saveFileAs(const QString& path) {", "    if (!buffer_.saveToFile(path.toStdString())) return false;", "    filePath_ = path;", "    setModified(false);", "    return true;", "}", "", "QString EditorWidget::filePath() const { return filePath_; }", "void EditorWidget::setFilePath(const QString& path) { filePath_ = path; }", "bool EditorWidget::isModified() const { return modified_; }", "QString EditorWidget::fileName() const {", "    if (filePath_.isEmpty()) return \"untitled\";", "    int sep = std::max(filePath_.lastIndexOf('/'), filePath_.lastIndexOf('\\\\'));", "    return filePath_.mid(sep + 1);", "}", "", "int EditorWidget::currentRow() const { return cursor_.row; }", "int EditorWidget::currentCol() const { return cursor_.col; }", "int EditorWidget::totalLines() const { return buffer_.lineCount(); }", "int EditorWidget::firstVisibleRow() const { return scrollY_ / charHeight_; }", "int EditorWidget::lastVisibleRow() const {", "    return std::min(firstVisibleRow() + height() / charHeight_ + 1, buffer_.lineCount() - 1);", "}", "int EditorWidget::getGutterWidth() const { return gutterWidth_; }", "int EditorWidget::getCharHeight() const { return charHeight_; }", "int EditorWidget::getScrollY() const { return scrollY_; }", "int EditorWidget::getCharWidth() const { return charWidth_; }", "", "void EditorWidget::setModified(bool m) {", "    if (modified_ != m) {", "        modified_ = m;", "        emit modifiedChanged(m);", "    }", "}", "", "void EditorWidget::updateGutterWidth() {", "    int digits = 1;", "    int lines = buffer_.lineCount();", "    while (lines >= 10) { digits++; lines /= 10; }", "    digits = std::max(digits, 3); // minimum 3 digits wide", "    gutterWidth_ = digits * charWidth_ + gutterPadding_ * 2;", "}", "", "void EditorWidget::resetCursorBlink() {", "    const QRect before = cursorRect();", "    cursorVisible_ = true;", "    blinkTimer_->start(500);   // restart, so the caret stays solid while typing", "    update(before);", "}", "", "void EditorWidget::toggleCursorBlink() {", "    cursorVisible_ = !cursorVisible_;", "    // Repaint the caret alone. Repainting the whole widget twice a second", "    // re-tokenised and re-drew every visible line, forever.", "    update(cursorRect());", "}", "", "void EditorWidget::rebuildCommentStateFull() {", "    const int lines = buffer_.lineCount();", "    blockCommentState_.assign(lines, false);", "    bool inComment = false;", "    for (int i = 0; i < lines; i++) {", "        blockCommentState_[i] = inComment;", "        highlighter_.tokenize(buffer_.line(i), inComment);", "    }", "}", "", "void EditorWidget::rebuildCommentState(int fromRow) {", "    const int lines = buffer_.lineCount();", "    if (lines == 0) { blockCommentState_.clear(); return; }", "", "    const int oldSize = static_cast<int>(blockCommentState_.size());", "    const int delta = lines - oldSize;", "    fromRow = std::clamp(fromRow, 0, lines - 1);", "", "    // Keep the cache index-aligned with the buffer when the edit added or", "    // removed lines, so the convergence test below compares like with like.", "    if (delta > 0) {", "        blockCommentState_.insert(blockCommentState_.begin() + std::min(fromRow, oldSize),", "                                  delta, false);", "    } else if (delta < 0) {", "        const int at = std::min(fromRow, lines);", "        blockCommentState_.erase(blockCommentState_.begin() + at,", "                                 blockCommentState_.begin() + at - delta);", "    }", "", "    // Rows before fromRow are untouched, so the cached state at fromRow is", "    // still whatever the previous line left behind.", "    bool inComment = blockCommentState_[fromRow];", "    const int settled = fromRow + std::abs(delta);   // shifted entries aren't trustworthy yet", "    for (int i = fromRow; i < lines; i++) {", "        // Once the recomputed state matches the cached one, every later line is", "        // provably unchanged and the scan can stop.", "        if (i > settled && blockCommentState_[i] == inComment) return;", "        blockCommentState_[i] = inComment;", "        highlighter_.tokenize(buffer_.line(i), inComment);", "    }", "}", "", "// -- Coordinate Conversion --", "", "int EditorWidget::rowFromY(int y) const {", "    return std::clamp((y + scrollY_) / charHeight_, 0, buffer_.lineCount() - 1);", "}", "", "int EditorWidget::colFromX(int x, int row) const {", "    // Round to the nearest boundary so a click on the right half of a glyph", "    // lands after it, the way every other editor behaves.", "    int col = (x - gutterWidth_ + scrollX_ + charWidth_ / 2) / charWidth_;", "    return std::clamp(col, 0, buffer_.lineLength(row));", "}", "", "int EditorWidget::xFromCol(int col) const {", "    return gutterWidth_ + col * charWidth_ - scrollX_;", "}", "", "int EditorWidget::yFromRow(int row) const {", "    return row * charHeight_ - scrollY_;", "}", "", "int EditorWidget::maxScrollY() const {", "    int totalHeight = buffer_.lineCount() * charHeight_;", "    return std::max(0, totalHeight - height() + charHeight_);", "}", "", "int EditorWidget::maxScrollX() const {", "    // Measuring every line would be O(file) on each scroll, so bound the", "    // extent by the widest line currently on screen plus the caret, which is", "    // all the user can actually reach from here.", "    int widest = cursor_.col;", "    const int first = std::max(0, scrollY_ / charHeight_);", "    const int last = std::min(first + height() / charHeight_ + 1, buffer_.lineCount() - 1);", "    for (int row = first; row <= last; row++) widest = std::max(widest, buffer_.lineLength(row));", "", "    const int viewport = std::max(1, width() - gutterWidth_);", "    return std::max(0, (widest + 2) * charWidth_ - viewport);", "}", "", "void EditorWidget::clampScroll() {", "    scrollY_ = std::clamp(scrollY_, 0, maxScrollY());", "    scrollX_ = std::clamp(scrollX_, 0, maxScrollX());", "}", "", "QRect EditorWidget::cursorRect() const {", "    // Slightly wider than the caret so the glow is included.", "    return QRect(xFromCol(cursor_.col) - 3, yFromRow(cursor_.row), 8, charHeight_);", "}", "", "void EditorWidget::animateScrollTo(int targetY) {", "    scrollTarget_ = std::clamp(targetY, 0, maxScrollY());", "    if (scrollTarget_ == scrollY_) return;", "", "    // Retarget an in-flight animation rather than restarting it from wherever", "    // it happens to be, so consecutive wheel ticks accumulate smoothly.", "    scrollAnim_->stop();", "    scrollAnim_->setStartValue(scrollY_);", "    scrollAnim_->setEndValue(scrollTarget_);", "    scrollAnim_->start();", "}", "", "void EditorWidget::jumpScrollTo(int y) {", "    scrollAnim_->stop();", "    scrollY_ = std::clamp(y, 0, maxScrollY());", "    scrollTarget_ = scrollY_;", "}", "", "void EditorWidget::ensureCursorVisible() {", "    // Keyboard navigation must be instant - animating the caret into view", "    // would make the editor feel like it is lagging behind the keystroke.", "    int cy = cursor_.row * charHeight_;", "    int y = scrollY_;", "    if (cy < y) {", "        y = cy;", "    } else if (cy + charHeight_ > y + height()) {", "        y = cy + charHeight_ - height();", "    }", "    if (y != scrollY_) jumpScrollTo(y);", "", "    // Horizontal: keep a few columns of context around the caret.", "    const int margin = 4 * charWidth_;", "    const int cx = gutterWidth_ + cursor_.col * charWidth_;", "    const int viewLeft = gutterWidth_ + scrollX_;", "    const int viewRight = width();", "    if (cx - margin < viewLeft) {", "        scrollX_ = std::max(0, cursor_.col * charWidth_ - margin);", "    } else if (cx - scrollX_ + margin > viewRight) {", "        scrollX_ = cursor_.col * charWidth_ + margin - (viewRight - gutterWidth_);", "    }", "    clampScroll();", "}", "", "// -- Selection Helpers --", "", "void EditorWidget::updateSelectionForMove(bool shift) {", "    if (shift) {", "        if (!selection_.active) {", "            selection_.start(cursor_);", "        }", "    } else {", "        selection_.clear();", "    }", "}", "", "void EditorWidget::deleteSelection() {", "    if (!selection_.hasSelection(cursor_)) return;", "    auto [start, end] = selection_.normalized(cursor_);", "", "    std::string text = buffer_.getText(start, end);", "    undoManager_.recordDelete(start, text);", "    undoManager_.forceNewGroup();", "", "    buffer_.deleteRange(start, end);", "    cursor_ = start;", "    selection_.clear();", "    setModified(true);", "    updateGutterWidth();", "    rebuildCommentState(start.row);", "}", "", "std::string EditorWidget::getSelectedText() const {", "    if (!selection_.hasSelection(cursor_)) return \"\";", "    auto [start, end] = selection_.normalized(cursor_);", "    return buffer_.getText(start, end);", "}", "", "// -- Cursor Movement --", "", "void EditorWidget::moveCursorLeft(bool shift) {", "    if (!shift && selection_.hasSelection(cursor_)) {", "        auto [start, end] = selection_.normalized(cursor_);", "        cursor_ = start;", "        selection_.clear();", "        return;", "    }", "    updateSelectionForMove(shift);", "    if (cursor_.col > 0) {", "        cursor_.col--;", "    } else if (cursor_.row > 0) {", "        cursor_.row--;", "        cursor_.col = buffer_.lineLength(cursor_.row);", "    }", "}", "", "void EditorWidget::moveCursorRight(bool shift) {", "    if (!shift && selection_.hasSelection(cursor_)) {", "        auto [start, end] = selection_.normalized(cursor_);", "        cursor_ = end;", "        selection_.clear();", "        return;", "    }", "    updateSelectionForMove(shift);", "    if (cursor_.col < buffer_.lineLength(cursor_.row)) {", "        cursor_.col++;", "    } else if (cursor_.row < buffer_.lineCount() - 1) {", "        cursor_.row++;", "        cursor_.col = 0;", "    }", "}", "", "void EditorWidget::moveCursorUp(bool shift) {", "    updateSelectionForMove(shift);", "    if (cursor_.row > 0) {", "        cursor_.row--;", "        cursor_.col = std::min(cursor_.col, buffer_.lineLength(cursor_.row));", "    }", "}", "", "void EditorWidget::moveCursorDown(bool shift) {", "    updateSelectionForMove(shift);", "    if (cursor_.row < buffer_.lineCount() - 1) {", "        cursor_.row++;", "        cursor_.col = std::min(cursor_.col, buffer_.lineLength(cursor_.row));", "    }", "}", "", "void EditorWidget::moveCursorHome(bool shift, bool ctrl) {", "    updateSelectionForMove(shift);", "    if (ctrl) {", "        cursor_ = {0, 0};", "    } else {", "        cursor_.col = 0;", "    }", "}", "", "void EditorWidget::moveCursorEnd(bool shift, bool ctrl) {", "    updateSelectionForMove(shift);", "    if (ctrl) {", "        cursor_.row = buffer_.lineCount() - 1;", "        cursor_.col = buffer_.lineLength(cursor_.row);", "    } else {", "        cursor_.col = buffer_.lineLength(cursor_.row);", "    }", "}", "", "void EditorWidget::moveCursorWordLeft(bool shift) {", "    updateSelectionForMove(shift);", "    if (cursor_.col == 0 && cursor_.row > 0) {", "        cursor_.row--;", "        cursor_.col = buffer_.lineLength(cursor_.row);", "    } else {", "        cursor_.col = buffer_.findWordBoundaryLeft(cursor_.row, cursor_.col);", "    }", "}", "", "void EditorWidget::moveCursorWordRight(bool shift) {", "    updateSelectionForMove(shift);", "    if (cursor_.col >= buffer_.lineLength(cursor_.row) && cursor_.row < buffer_.lineCount() - 1) {", "        cursor_.row++;", "        cursor_.col = 0;", "    } else {", "        cursor_.col = buffer_.findWordBoundaryRight(cursor_.row, cursor_.col);", "    }", "}", "", "// -- Indentation & Bracket Intelligence --", "", "char EditorWidget::closerFor(char open) {", "    switch (open) {", "        case '(':  return ')';", "        case '[':  return ']';", "        case '{':  return '}';", "        case '\"':  return '\"';", "        case '\\'': return '\\'';", "        default:   return 0;", "    }", "}", "", "bool EditorWidget::isCloser(char c) {", "    return c == ')' || c == ']' || c == '}' || c == '\"' || c == '\\'';", "}", "", "bool EditorWidget::isIdentLike(char c) {", "    return (c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z') ||", "           (c >= '0' && c <= '9') || c == '_';", "}", "", "int EditorWidget::indentWidthOf(const std::string& line) const {", "    int width = 0;", "    for (char c : line) {", "        if (c == ' ')       width++;", "        else if (c == '\\t') width += INDENT_WIDTH - (width % INDENT_WIDTH);", "        else break;", "    }", "    return width;", "}", "", "bool EditorWidget::onlyWhitespaceBefore(int row, int col) const {", "    const std::string& line = buffer_.line(row);", "    for (int i = 0; i < col && i < (int)line.size(); i++) {", "        if (line[i] != ' ' && line[i] != '\\t') return false;", "    }", "    return true;", "}", "", "bool EditorWidget::findMatchingOpenBrace(Position closePos, Position& out) const {", "    int depth = 0;", "    for (int row = closePos.row; row >= 0; row--) {", "        const std::string& line = buffer_.line(row);", "", "        // Braces inside a string literal or a comment are not structure. Reuse", "        // the highlighter so this agrees with what the user actually sees.", "        bool inComment = (row < (int)blockCommentState_.size()) ? blockCommentState_[row] : false;", "        auto tokens = highlighter_.tokenize(line, inComment);", "        std::vector<bool> isCode(line.size(), false);", "        for (const auto& tok : tokens) {", "            bool code = (tok.type != TokenType::String && tok.type != TokenType::Comment);", "            for (int i = 0; i < tok.length; i++) {", "                int c = tok.start + i;", "                if (c >= 0 && c < (int)line.size()) isCode[c] = code;", "            }", "        }", "", "        int startCol = (row == closePos.row) ? closePos.col - 1 : (int)line.size() - 1;", "        for (int c = std::min(startCol, (int)line.size() - 1); c >= 0; c--) {", "            if (!isCode[c]) continue;", "            if (line[c] == '}') {", "                depth++;", "            } else if (line[c] == '{') {", "                if (depth == 0) { out = {row, c}; return true; }", "                depth--;", "            }", "        }", "    }", "    return false;", "}", "", "void EditorWidget::reindentClosingBrace() {", "    int row = cursor_.row;", "    int bracePos = cursor_.col - 1;          // cursor sits just after the '}'", "    if (bracePos < 0) return;", "    const std::string& line = buffer_.line(row);", "    if (bracePos >= (int)line.size() || line[bracePos] != '}') return;", "", "    // Only realign a brace that opens its own line. A '}' typed after code", "    // (`} else {`, `int a[] = {1};`) must be left exactly where it was typed.", "    if (!onlyWhitespaceBefore(row, bracePos)) return;", "", "    Position openPos;", "    int desired;", "    if (findMatchingOpenBrace({row, bracePos}, openPos)) {", "        desired = indentWidthOf(buffer_.line(openPos.row));", "    } else {", "        // Unbalanced source - fall back to one level shallower than we are.", "        desired = std::max(0, indentWidthOf(line) - INDENT_WIDTH);", "    }", "", "    std::string desiredWs(desired, ' ');", "    if ((int)desiredWs.size() == bracePos && line.compare(0, bracePos, desiredWs) == 0) {", "        return;                               // already correct", "    }", "", "    Position start{row, 0};", "    Position end{row, bracePos};", "    std::string removed = buffer_.getText(start, end);", "    if (!removed.empty()) {", "        undoManager_.recordDelete(start, removed);", "        buffer_.deleteRange(start, end);", "    }", "    if (!desiredWs.empty()) {", "        undoManager_.recordInsert(start, desiredWs);", "        buffer_.insertText(row, 0, desiredWs);", "    }", "    cursor_.col = desired + 1;                // immediately after the '}'", "}", "", "void EditorWidget::adjustColAfterIndent(int row, int delta) {", "    if (cursor_.row == row) cursor_.col = std::max(0, cursor_.col + delta);", "    if (selection_.active && selection_.anchor.row == row) {", "        selection_.anchor.col = std::max(0, selection_.anchor.col + delta);", "    }", "}", "", "void EditorWidget::indentBlock(int firstRow, int lastRow, bool unindent) {", "    // One undo step for the whole block, so Ctrl+Z undoes the indent the user", "    // applied rather than unpicking it a line at a time.", "    undoManager_.beginCompound();", "", "    for (int row = firstRow; row <= lastRow && row < buffer_.lineCount(); row++) {", "        const std::string& line = buffer_.line(row);", "", "        if (unindent) {", "            int strip = 0;", "            while (strip < INDENT_WIDTH && strip < (int)line.size() && line[strip] == ' ') strip++;", "            if (strip == 0 && !line.empty() && line[0] == '\\t') strip = 1;", "            if (strip == 0) continue;", "", "            Position s{row, 0}, e{row, strip};", "            undoManager_.recordDelete(s, buffer_.getText(s, e));", "            buffer_.deleteRange(s, e);", "            adjustColAfterIndent(row, -strip);", "        } else {", "            if (line.empty()) continue;       // don't leave whitespace on blank lines", "            std::string pad(INDENT_WIDTH, ' ');", "            undoManager_.recordInsert({row, 0}, pad);", "            buffer_.insertText(row, 0, pad);", "            adjustColAfterIndent(row, INDENT_WIDTH);", "        }", "    }", "", "    undoManager_.endCompound();", "}", "", "// -- Snippets -----------------------------------------------------------------", "", "QString EditorWidget::wordBeforeCursor(int* startCol) const {", "    const std::string& line = buffer_.line(cursor_.row);", "    int end = std::min(cursor_.col, (int)line.size());", "    int start = end;", "    while (start > 0 && isIdentLike(line[start - 1])) start--;", "    if (startCol) *startCol = start;", "    return QString::fromLatin1(line.data() + start, end - start);", "}", "", "void EditorWidget::updateSnippetSuggestion() {", "    // A selection means the user is doing something other than typing a word.", "    if (selection_.hasSelection(cursor_)) { hideSnippetSuggestion(); return; }", "", "    const QString word = wordBeforeCursor();", "    const QVector<Snippet> hits = Snippets::matching(word);", "    if (hits.isEmpty()) { hideSnippetSuggestion(); return; }", "", "    // Anchor under the caret's line, at the start of the word being completed.", "    int startCol = 0;", "    wordBeforeCursor(&startCol);", "    const QPoint anchor(xFromCol(startCol), yFromRow(cursor_.row) + charHeight_ + 2);", "    snippetPopup_->showFor(hits, anchor);", "}", "", "void EditorWidget::hideSnippetSuggestion() {", "    if (snippetPopup_ && snippetPopup_->isVisible()) snippetPopup_->hide();", "}", "", "bool EditorWidget::acceptSnippet() {", "    if (!snippetPopup_ || !snippetPopup_->isVisible()) return false;", "    const Snippet* snippet = snippetPopup_->selected();", "    if (!snippet) return false;", "", "    int startCol = 0;", "    const QString word = wordBeforeCursor(&startCol);", "    hideSnippetSuggestion();", "    if (word.isEmpty()) return false;", "", "    // The whole expansion is one undo step: Ctrl+Z takes the template away and", "    // leaves the trigger the user typed.", "    undoManager_.beginCompound();", "", "    const Position wordStart{cursor_.row, startCol};", "    const std::string removed = buffer_.getText(wordStart, cursor_);", "    undoManager_.recordDelete(wordStart, removed);", "    buffer_.deleteRange(wordStart, cursor_);", "    cursor_ = wordStart;", "", "    // Split at the caret marker: insert what comes before it, remember where", "    // that landed, then append the remainder after it.", "    const int markerAt = snippet->body.indexOf(Snippets::Caret);", "    const QString before = markerAt >= 0 ? snippet->body.left(markerAt) : snippet->body;", "    const QString after  = markerAt >= 0 ? snippet->body.mid(markerAt + 1) : QString();", "", "    const std::string beforeUtf8 = before.toStdString();", "    undoManager_.recordInsert(cursor_, beforeUtf8);", "    const Position caret = buffer_.insertText(cursor_.row, cursor_.col, beforeUtf8);", "", "    if (!after.isEmpty()) {", "        const std::string afterUtf8 = after.toStdString();", "        undoManager_.recordInsert(caret, afterUtf8);", "        buffer_.insertText(caret.row, caret.col, afterUtf8);", "    }", "", "    undoManager_.endCompound();", "", "    cursor_ = caret;", "    selection_.clear();", "    setModified(true);", "    updateGutterWidth();", "    rebuildCommentState(wordStart.row);", "    return true;", "}", "", "// -- Edit Operations --", "", "void EditorWidget::handleChar(char ch) {", "    if (selection_.hasSelection(cursor_)) deleteSelection();", "", "    const std::string& line = buffer_.line(cursor_.row);", "    char nextCh = (cursor_.col < (int)line.size()) ? line[cursor_.col] : '\\0';", "    char prevCh = (cursor_.col > 0) ? line[cursor_.col - 1] : '\\0';", "", "    // Typing a closer that is already sitting under the cursor steps over it", "    // instead of doubling it. Without this, auto-close makes ')' unusable.", "    if (isCloser(ch) && nextCh == ch) {", "        cursor_.col++;", "        return;", "    }", "", "    if (ch == '}') undoManager_.beginCompound();", "", "    buffer_.insertChar(cursor_.row, cursor_.col, ch);", "    undoManager_.recordInsert(cursor_, std::string(1, ch));", "    cursor_.col++;", "", "    if (ch == '}') {", "        reindentClosingBrace();", "        undoManager_.endCompound();", "    } else if (char closing = closerFor(ch)) {", "        // Auto-closing into the middle of a word is never what was meant, and", "        // \"don't\" must not become \"don''t\".", "        bool suppress = isIdentLike(nextCh) ||", "                        ((ch == '\"' || ch == '\\'') && isIdentLike(prevCh));", "        if (!suppress) {", "            buffer_.insertChar(cursor_.row, cursor_.col, closing);", "            undoManager_.recordInsert({cursor_.row, cursor_.col}, std::string(1, closing));", "            // Cursor stays between the pair", "        }", "    }", "", "    setModified(true);", "    rebuildCommentState(cursor_.row);", "}", "", "void EditorWidget::handleBackspace(bool ctrl) {", "    if (selection_.hasSelection(cursor_)) {", "        deleteSelection();", "        return;", "    }", "", "    if (ctrl) {", "        // Delete word backward", "        int newCol = buffer_.findWordBoundaryLeft(cursor_.row, cursor_.col);", "        if (newCol == cursor_.col && cursor_.col == 0 && cursor_.row > 0) {", "            // Merge with previous line", "            int prevLen = buffer_.lineLength(cursor_.row - 1);", "            std::string deleted = \"\\n\";", "            undoManager_.recordDelete({cursor_.row - 1, prevLen}, deleted);", "            undoManager_.forceNewGroup();", "            buffer_.mergeLines(cursor_.row);", "            cursor_.row--;", "            cursor_.col = prevLen;", "        } else {", "            Position start = {cursor_.row, newCol};", "            Position end = cursor_;", "            std::string text = buffer_.getText(start, end);", "            undoManager_.recordDelete(start, text);", "            undoManager_.forceNewGroup();", "            buffer_.deleteRange(start, end);", "            cursor_.col = newCol;", "        }", "    } else {", "        if (cursor_.col > 0) {", "            const std::string& l = buffer_.line(cursor_.row);", "            char left  = l[cursor_.col - 1];", "            char right = (cursor_.col < (int)l.size()) ? l[cursor_.col] : '\\0';", "", "            // Backspacing out of an empty pair the editor auto-inserted removes", "            // both halves; leaving the orphaned closer behind is the single", "            // most irritating failure mode of auto-close.", "            if (right != '\\0' && closerFor(left) == right) {", "                Position start{cursor_.row, cursor_.col - 1};", "                Position end{cursor_.row, cursor_.col + 1};", "                undoManager_.recordDelete(start, buffer_.getText(start, end));", "                undoManager_.forceNewGroup();", "                buffer_.deleteRange(start, end);", "                cursor_.col--;", "                setModified(true);", "                updateGutterWidth();", "                rebuildCommentState(cursor_.row);", "                return;", "            }", "", "            std::string ch(1, left);", "            undoManager_.recordDelete({cursor_.row, cursor_.col - 1}, ch);", "            buffer_.deleteChar(cursor_.row, cursor_.col);", "            cursor_.col--;", "        } else if (cursor_.row > 0) {", "            int prevLen = buffer_.lineLength(cursor_.row - 1);", "            undoManager_.recordDelete({cursor_.row - 1, prevLen}, \"\\n\");", "            undoManager_.forceNewGroup();", "            buffer_.mergeLines(cursor_.row);", "            cursor_.row--;", "            cursor_.col = prevLen;", "        }", "    }", "", "    setModified(true);", "    updateGutterWidth();", "    rebuildCommentState(cursor_.row);", "}", "", "void EditorWidget::handleDelete() {", "    if (selection_.hasSelection(cursor_)) {", "        deleteSelection();", "        return;", "    }", "", "    if (cursor_.col < buffer_.lineLength(cursor_.row)) {", "        std::string ch(1, buffer_.line(cursor_.row)[cursor_.col]);", "        undoManager_.recordDelete(cursor_, ch);", "        // Delete forward: erase char at cursor position", "        std::string& line = const_cast<std::string&>(buffer_.line(cursor_.row));", "        line.erase(cursor_.col, 1);", "    } else if (cursor_.row < buffer_.lineCount() - 1) {", "        undoManager_.recordDelete(cursor_, \"\\n\");", "        undoManager_.forceNewGroup();", "        // mergeLines(row+1) appends line[row+1] to line[row] and erases line[row+1]", "        buffer_.mergeLines(cursor_.row + 1);", "    }", "", "    setModified(true);", "    updateGutterWidth();", "    rebuildCommentState(cursor_.row);", "}", "", "void EditorWidget::handleEnter() {", "    if (selection_.hasSelection(cursor_)) deleteSelection();", "", "    const int startRow = cursor_.row;", "    std::string indent = buffer_.getLeadingWhitespace(cursor_.row);", "", "    // Look at the last real character before the cursor, not the character", "    // immediately before it - otherwise `if (x) {` + Enter fails to indent,", "    // because auto-close has left the cursor sitting between '{' and '}'.", "    const std::string& currentLine = buffer_.line(cursor_.row);", "    char lastCode = '\\0';", "    for (int i = std::min(cursor_.col, (int)currentLine.size()) - 1; i >= 0; i--) {", "        if (currentLine[i] != ' ' && currentLine[i] != '\\t') { lastCode = currentLine[i]; break; }", "    }", "    char nextCode = '\\0';", "    for (int i = cursor_.col; i < (int)currentLine.size(); i++) {", "        if (currentLine[i] != ' ' && currentLine[i] != '\\t') { nextCode = currentLine[i]; break; }", "    }", "", "    bool opensBlock = (lastCode == '{');", "    bool isBetweenBraces = opensBlock && (nextCode == '}');", "", "    std::string nextLineIndent = indent;", "    if (opensBlock) {", "        nextLineIndent += std::string(INDENT_WIDTH, ' ');", "    }", "", "    if (isBetweenBraces) {", "        std::string textToInsert = \"\\n\" + nextLineIndent + \"\\n\" + indent;", "        undoManager_.recordInsert(cursor_, textToInsert);", "        undoManager_.forceNewGroup();", "", "        buffer_.insertText(cursor_.row, cursor_.col, textToInsert);", "", "        // Place cursor on the newly created middle line", "        cursor_.row++;", "        cursor_.col = nextLineIndent.length();", "    } else {", "        std::string textToInsert = \"\\n\" + nextLineIndent;", "        undoManager_.recordInsert(cursor_, textToInsert);", "        undoManager_.forceNewGroup();", "", "        cursor_ = buffer_.insertText(cursor_.row, cursor_.col, textToInsert);", "    }", "", "    setModified(true);", "    updateGutterWidth();", "    rebuildCommentState(startRow);", "}", "", "void EditorWidget::handleTab(bool shift) {", "    if (selection_.hasSelection(cursor_)) {", "        auto [start, end] = selection_.normalized(cursor_);", "        int lastRow = end.row;", "        // A selection ending at column 0 stops short of that line; indenting it", "        // would shift a line the user never highlighted.", "        if (end.col == 0 && lastRow > start.row) lastRow--;", "", "        if (shift || lastRow > start.row) {", "            indentBlock(start.row, lastRow, shift);", "            setModified(true);", "            updateGutterWidth();", "            rebuildCommentState(start.row);", "            return;                           // selection survives, so Tab repeats", "        }", "        deleteSelection();                    // single-line selection: replace it", "    } else if (shift) {", "        indentBlock(cursor_.row, cursor_.row, true);", "        setModified(true);", "        rebuildCommentState(cursor_.row);", "        return;", "    }", "", "    std::string spaces(INDENT_WIDTH, ' ');", "    Position tabStart = cursor_;              // record position BEFORE inserting", "    buffer_.insertText(cursor_.row, cursor_.col, spaces);", "    cursor_.col += INDENT_WIDTH;", "    undoManager_.recordInsert(tabStart, spaces);", "    undoManager_.forceNewGroup();", "    setModified(true);", "    rebuildCommentState(cursor_.row);", "}", "", "void EditorWidget::movePage(int direction, bool shift) {", "    updateSelectionForMove(shift);", "    int rows = std::max(1, height() / charHeight_ - 1);", "    cursor_.row = std::clamp(cursor_.row + direction * rows, 0, buffer_.lineCount() - 1);", "    cursor_.col = std::min(cursor_.col, buffer_.lineLength(cursor_.row));", "    scrollY_ = std::clamp(scrollY_ + direction * rows * charHeight_, 0, maxScrollY());", "}", "", "// -- Clipboard --", "", "void EditorWidget::copy() {", "    std::string text = getSelectedText();", "    if (!text.empty()) {", "        QApplication::clipboard()->setText(QString::fromStdString(text));", "    }", "}", "", "void EditorWidget::cut() {", "    copy();", "    if (selection_.hasSelection(cursor_)) {", "        deleteSelection();", "    }", "}", "", "void EditorWidget::paste() {", "    QString clipText = QApplication::clipboard()->text();", "    if (clipText.isEmpty()) return;", "", "    if (selection_.hasSelection(cursor_)) deleteSelection();", "", "    const int pasteRow = cursor_.row;", "    std::string text = clipText.toStdString();", "    undoManager_.recordInsert(cursor_, text);", "    undoManager_.forceNewGroup();", "", "    Position newPos = buffer_.insertText(cursor_.row, cursor_.col, text);", "    cursor_ = newPos;", "", "    setModified(true);", "    updateGutterWidth();", "    rebuildCommentState(pasteRow);", "}", "", "void EditorWidget::selectAll() {", "    selection_.anchor = {0, 0};", "    selection_.active = true;", "    cursor_.row = buffer_.lineCount() - 1;", "    cursor_.col = buffer_.lineLength(cursor_.row);", "}", "", "void EditorWidget::setSelection(int startRow, int startCol, int endRow, int endCol) {", "    selection_.anchor = {startRow, startCol};", "    selection_.active = true;", "    cursor_ = {endRow, endCol};", "    ensureCursorVisible();", "    resetCursorBlink();", "    emit cursorPositionChanged(cursor_.row, cursor_.col);", "    update();", "}", "", "// -- Undo / Redo --", "", "void EditorWidget::performUndo() {", "    auto result = undoManager_.undo();", "    if (!result.valid) return;", "", "    for (auto& action : result.actions) {", "        if (action.type == EditAction::Insert) {", "            // Undo insert = delete the text", "            // The text was inserted at action.pos, so delete from action.pos for text.length", "            Position start = action.pos;", "            // Calculate end position from text", "            int row = start.row, col = start.col;", "            for (char c : action.text) {", "                if (c == '\\n') { row++; col = 0; }", "                else col++;", "            }", "            Position endPos = {row, col};", "            buffer_.deleteRange(start, endPos);", "            cursor_ = start;", "        } else {", "            // Undo delete = re-insert the text", "            buffer_.insertText(action.pos.row, action.pos.col, action.text);", "            // Position cursor at end of re-inserted text", "            int row = action.pos.row, col = action.pos.col;", "            for (char c : action.text) {", "                if (c == '\\n') { row++; col = 0; }", "                else col++;", "            }", "            cursor_ = {row, col};", "        }", "    }", "", "    selection_.clear();", "    setModified(true);", "    updateGutterWidth();", "    rebuildCommentStateFull();", "}", "", "void EditorWidget::performRedo() {", "    auto result = undoManager_.redo();", "    if (!result.valid) return;", "", "    for (auto& action : result.actions) {", "        if (action.type == EditAction::Insert) {", "            Position newPos = buffer_.insertText(action.pos.row, action.pos.col, action.text);", "            cursor_ = newPos;", "        } else {", "            Position start = action.pos;", "            int row = start.row, col = start.col;", "            for (char c : action.text) {", "                if (c == '\\n') { row++; col = 0; }", "                else col++;", "            }", "            buffer_.deleteRange(start, {row, col});", "            cursor_ = start;", "        }", "    }", "", "    selection_.clear();", "    setModified(true);", "    updateGutterWidth();", "    rebuildCommentStateFull();", "}", "", "// -- Input Handling --", "", "bool EditorWidget::event(QEvent* e) {", "    // Intercept Tab/Backtab before Qt uses them for focus navigation", "    if (e->type() == QEvent::KeyPress) {", "        QKeyEvent* ke = static_cast<QKeyEvent*>(e);", "        if (ke->key() == Qt::Key_Tab || ke->key() == Qt::Key_Backtab) {", "            keyPressEvent(ke);", "            return true;", "        }", "    }", "    return QWidget::event(e);", "}", "", "void EditorWidget::keyPressEvent(QKeyEvent* e) {", "    bool ctrl = e->modifiers() & Qt::ControlModifier;", "    bool shift = e->modifiers() & Qt::ShiftModifier;", "", "    if (snippetPopup_->isVisible() && !ctrl) {", "        switch (e->key()) {", "        case Qt::Key_Escape:", "            hideSnippetSuggestion();", "            return;", "        case Qt::Key_Up:", "            snippetPopup_->moveSelection(-1);", "            return;", "        case Qt::Key_Down:", "            snippetPopup_->moveSelection(1);", "            return;", "        case Qt::Key_Tab:", "        case Qt::Key_Return:", "        case Qt::Key_Enter:", "            if (acceptSnippet()) {", "                ensureCursorVisible();", "                resetCursorBlink();", "                emit cursorPositionChanged(cursor_.row, cursor_.col);", "                update();", "                return;", "            }", "            break;", "        default:", "            break;", "        }", "    }", "", "    if (ctrl) {", "        // -- Ctrl+ shortcuts --", "        switch (e->key()) {", "        case Qt::Key_Z:     if (shift) performRedo(); else performUndo(); break;", "        case Qt::Key_Y:     performRedo(); break;", "        case Qt::Key_C:     copy(); break;", "        case Qt::Key_X:     cut(); break;", "        case Qt::Key_V:     paste(); break;", "        case Qt::Key_A:     selectAll(); break;", "        case Qt::Key_S:     emit saveRequested(); break;", "        case Qt::Key_Left:  moveCursorWordLeft(shift); break;", "        case Qt::Key_Right: moveCursorWordRight(shift); break;", "        case Qt::Key_Home:  moveCursorHome(shift, true); break;", "        case Qt::Key_End:   moveCursorEnd(shift, true); break;", "        case Qt::Key_Backspace: handleBackspace(true); break;", "        default: QWidget::keyPressEvent(e); return;", "        }", "    } else {", "        // -- Normal keys --", "        switch (e->key()) {", "        case Qt::Key_Left:      moveCursorLeft(shift); break;", "        case Qt::Key_Right:     moveCursorRight(shift); break;", "        case Qt::Key_Up:        moveCursorUp(shift); break;", "        case Qt::Key_Down:      moveCursorDown(shift); break;", "        case Qt::Key_Home:      moveCursorHome(shift, false); break;", "        case Qt::Key_End:       moveCursorEnd(shift, false); break;", "        case Qt::Key_Backspace: handleBackspace(false); break;", "        case Qt::Key_Delete:    handleDelete(); break;", "        case Qt::Key_Return:", "        case Qt::Key_Enter:     handleEnter(); break;", "        case Qt::Key_Tab:       handleTab(false); break;", "        case Qt::Key_Backtab:   handleTab(true); break;", "        case Qt::Key_PageUp:    movePage(-1, shift); break;", "        case Qt::Key_PageDown:  movePage(1, shift); break;", "        default:", "            if (!e->text().isEmpty()) {", "                QChar ch = e->text().at(0);", "                // TextBuffer is byte-indexed and the renderer assumes one byte", "                // per column, so a non-ASCII character cannot be represented", "                // without desynchronising every column calculation. Drop it", "                // rather than inserting the NUL byte toLatin1() would produce.", "                if (ch.isPrint() && ch.unicode() < 128) {", "                    handleChar(static_cast<char>(ch.unicode()));", "                }", "            }", "            break;", "        }", "    }", "", "    // Offer a snippet only while a word is actively being typed or deleted;", "    // any other key dismisses the list.", "    switch (e->key()) {", "    case Qt::Key_Backspace:", "    case Qt::Key_Delete:", "        updateSnippetSuggestion();", "        break;", "    default:", "        if (!ctrl && !e->text().isEmpty() && isIdentLike(e->text().at(0).toLatin1())) {", "            updateSnippetSuggestion();", "        } else {", "            hideSnippetSuggestion();", "        }", "        break;", "    }", "", "    ensureCursorVisible();", "    resetCursorBlink();", "    emit cursorPositionChanged(cursor_.row, cursor_.col);", "    update();", "}", "", "// -- Mouse Handling --", "", "void EditorWidget::mousePressEvent(QMouseEvent* e) {", "    hideSnippetSuggestion();", "    if (e->button() == Qt::LeftButton && e->position().x() > gutterWidth_) {", "        int row = rowFromY((int)e->position().y());", "        int col = colFromX((int)e->position().x(), row);", "        cursor_ = {row, col};", "        selection_.clear();", "        selection_.start(cursor_);", "        resetCursorBlink();", "        emit cursorPositionChanged(cursor_.row, cursor_.col);", "        update();", "    }", "}", "", "void EditorWidget::mouseDoubleClickEvent(QMouseEvent* e) {", "    if (e->button() != Qt::LeftButton) return;", "", "    const int row = rowFromY((int)e->position().y());", "    const int col = colFromX((int)e->position().x(), row);", "", "    if (e->type() == QEvent::MouseButtonDblClick) {", "        const int left = buffer_.findWordBoundaryLeft(row, std::min(col + 1, buffer_.lineLength(row)));", "        const int right = buffer_.findWordBoundaryRight(row, col);", "        if (right > left) {", "            setSelection(row, left, row, right);", "            return;", "        }", "    }", "    cursor_ = {row, col};", "    selection_.clear();", "    resetCursorBlink();", "    emit cursorPositionChanged(cursor_.row, cursor_.col);", "    update();", "}", "", "void EditorWidget::mouseMoveEvent(QMouseEvent* e) {", "    if (!(e->buttons() & Qt::LeftButton)) return;", "", "    const int y = (int)e->position().y();", "    int row = rowFromY(y);", "    int col = colFromX((int)e->position().x(), row);", "    cursor_ = {row, col};", "", "    // Dragging past the top or bottom edge keeps extending the selection", "    // instead of stopping dead at the last visible line.", "    if (y < 0) {", "        jumpScrollTo(scrollY_ - charHeight_);", "    } else if (y > height()) {", "        jumpScrollTo(scrollY_ + charHeight_);", "    }", "", "    ensureCursorVisible();", "    resetCursorBlink();", "    emit cursorPositionChanged(cursor_.row, cursor_.col);", "    update();", "}", "", "void EditorWidget::wheelEvent(QWheelEvent* e) {", "    const QPoint angle = e->angleDelta();", "", "    if (e->modifiers() & Qt::ControlModifier) {", "        const int steps = angle.y() / 120;", "        if (steps != 0) emit zoomStepRequested(steps);", "        e->accept();", "        return;", "    }", "", "    // Shift+wheel pans horizontally, as does a real horizontal wheel.", "    if ((e->modifiers() & Qt::ShiftModifier) || angle.x() != 0) {", "        const int delta = (angle.x() != 0) ? angle.x() : angle.y();", "        scrollX_ -= (delta * 3 * charWidth_) / 120;", "        clampScroll();", "        update();", "        e->accept();", "        return;", "    }", "", "    if (angle.y() == 0) { QWidget::wheelEvent(e); return; }", "", "    hideSnippetSuggestion();", "    const int step = (angle.y() * 3 * charHeight_) / 120;", "    animateScrollTo(scrollTarget_ - step);", "    e->accept();", "}", "", "void EditorWidget::resizeEvent(QResizeEvent* e) {", "    QWidget::resizeEvent(e);", "    clampScroll();", "    scrollTarget_ = scrollY_;   // a resize can clamp the view out from under an animation", "}", "", "// -- Paint --", "", "void EditorWidget::paintEvent(QPaintEvent* event) {", "    QPainter p(this);", "    p.setRenderHint(QPainter::TextAntialiasing);", "    p.setFont(font_);", "", "    // Only touch what was invalidated. A caret blink then costs one small", "    // rectangle instead of the entire viewport.", "    const QRect clip = event->rect();", "    p.fillRect(clip, Theme::EditorBg);", "", "    int startRow = std::max(0, (scrollY_ + clip.top()) / charHeight_);", "    int endRow = std::min((scrollY_ + clip.bottom()) / charHeight_ + 1, buffer_.lineCount());", "", "    // Code is clipped to its own column so horizontally scrolled text cannot", "    // bleed underneath the gutter.", "    p.save();", "    p.setClipRect(QRect(gutterWidth_, 0, std::max(0, width() - gutterWidth_), height()), Qt::IntersectClip);", "    paintCode(p, startRow, endRow);", "    paintCursor(p);", "    p.restore();", "", "    paintGutter(p, startRow, endRow);", "}", "", "void EditorWidget::paintGutter(QPainter& p, int startRow, int endRow) {", "    // The gutter shares the editor's surface - no band, no rule.", "    p.fillRect(0, 0, gutterWidth_, height(), Theme::EditorBg);", "    p.setFont(font_);", "    for (int row = startRow; row < endRow; row++) {", "        int y = yFromRow(row);", "        p.setPen(row == cursor_.row ? Theme::TextSecondary : Theme::TextMuted);", "        QString num = QString::number(row + 1);", "        int x = gutterWidth_ - gutterPadding_ - p.fontMetrics().horizontalAdvance(num);", "        p.drawText(x, y + ascent_, num);", "    }", "}", "", "void EditorWidget::paintCode(QPainter& p, int startRow, int endRow) {", "    p.setFont(font_);", "", "    for (int row = startRow; row < endRow; row++) {", "        int y = yFromRow(row);", "", "        // Current line highlight", "        if (row == cursor_.row) {", "            p.fillRect(gutterWidth_, y, width() - gutterWidth_, charHeight_, Theme::CurrentLine);", "        }", "", "        // Selection highlight", "        paintSelection(p, row, y);", "", "        // Tokenize and draw", "        bool inComment = (row < (int)blockCommentState_.size()) ? blockCommentState_[row] : false;", "        const std::string& lineStr = buffer_.line(row);", "        auto tokens = highlighter_.tokenize(lineStr, inComment);", "", "        // Build the line's QString once. The previous code allocated a", "        // std::string AND a QString per character per frame - roughly 14,000", "        // allocations for a full viewport, every repaint.", "        // Latin-1 keeps a strict one-byte-one-column mapping, which is what the", "        // caret arithmetic throughout this class assumes.", "        const QString lineText = QString::fromLatin1(lineStr.data(), (int)lineStr.size());", "", "        // Skip tokens scrolled off either edge.", "        const int firstCol = std::max(0, scrollX_ / charWidth_ - 1);", "        const int lastCol = firstCol + (width() - gutterWidth_) / charWidth_ + 2;", "", "        for (const auto& tok : tokens) {", "            if (tok.start + tok.length < firstCol || tok.start > lastCol) continue;", "            QColor color;", "            switch (tok.type) {", "            case TokenType::Keyword:      color = Theme::SynKeyword; break;", "            case TokenType::Type:         color = Theme::SynType; break;", "            case TokenType::String:       color = Theme::SynString; break;", "            case TokenType::Comment:      color = Theme::SynComment; break;", "            case TokenType::Number:       color = Theme::SynNumber; break;", "            case TokenType::Preprocessor: color = Theme::SynPreprocessor; break;", "            case TokenType::Function:     color = Theme::SynFunction; break;", "            case TokenType::Punctuation:  color = Theme::SynPunctuation; break;", "            default:                      color = Theme::TextPrimary; break;", "            }", "", "            p.setPen(color);", "            if (monospaceExact_) {", "                // The font's advances were verified against the grid at", "                // construction, so the whole token can go out in one call.", "                p.drawText(xFromCol(tok.start), y + ascent_,", "                           lineText.mid(tok.start, tok.length));", "            } else {", "                // A fallback font is in play and its advances do not match the", "                // grid. Place each glyph by hand instead, which is slower but", "                // keeps text and caret aligned - the \"ghost character\" bug.", "                for (int i = 0; i < tok.length; i++) {", "                    p.drawText(xFromCol(tok.start + i), y + ascent_,", "                               lineText.mid(tok.start + i, 1));", "                }", "            }", "        }", "    }", "}", "", "void EditorWidget::paintSelection(QPainter& p, int row, int y) {", "    if (!selection_.hasSelection(cursor_)) return;", "", "    auto [selStart, selEnd] = selection_.normalized(cursor_);", "    if (row < selStart.row || row > selEnd.row) return;", "", "    int x1, x2;", "    if (row == selStart.row && row == selEnd.row) {", "        x1 = xFromCol(selStart.col);", "        x2 = xFromCol(selEnd.col);", "    } else if (row == selStart.row) {", "        x1 = xFromCol(selStart.col);", "        x2 = xFromCol(buffer_.lineLength(row)) + charWidth_;", "    } else if (row == selEnd.row) {", "        x1 = gutterWidth_;", "        x2 = xFromCol(selEnd.col);", "    } else {", "        x1 = gutterWidth_;", "        x2 = xFromCol(buffer_.lineLength(row)) + charWidth_;", "    }", "", "    p.fillRect(x1, y, x2 - x1, charHeight_, Theme::SelectionBg);", "}", "", "void EditorWidget::paintCursor(QPainter& p) {", "    if (!cursorVisible_) return;", "    if (cursor_.row < firstVisibleRow() || cursor_.row > lastVisibleRow()) return;", "", "    int cx = xFromCol(cursor_.col);", "    int cy = yFromRow(cursor_.row);", "", "    // Glow", "    p.fillRect(cx - 2, cy, 6, charHeight_, Theme::AccentGlow);", "    // Cursor line", "    p.fillRect(cx, cy, 2, charHeight_, Theme::Accent);", "}", "#pragma once", "#include <QString>", "", "// A single competitive-programming test case and the result of judging it.", "// Kept as a plain value type so the UI can copy it around freely.", "", "enum class Verdict {", "    Pending,        // never run, or edited since the last run", "    Running,        // process is live right now", "    Accepted,       // stdout matched expected", "    WrongAnswer,    // ran to completion, output differed", "    TimeLimit,      // exceeded the time limit and was killed", "    RuntimeError,   // non-zero exit or crashed", "    CompileError    // the translation unit never built", "};", "", "struct TestCase {", "    QString input;        // fed to the process on stdin", "    QString expected;     // what stdout should contain", "    QString actual;       // what stdout actually contained (filled by Judge)", "    QString errorText;    // stderr, or the compiler diagnostic on CompileError", "    Verdict verdict = Verdict::Pending;", "    int elapsedMs = 0;", "    int exitCode = 0;", "};", "", "// Display helpers - shared by the card badges and the summary line so a", "// verdict is never spelled two different ways in two different places.", "inline QString verdictLabel(Verdict v) {", "    switch (v) {", "    case Verdict::Pending:      return QStringLiteral(\"-\");", "    case Verdict::Running:      return QStringLiteral(\"RUN\");", "    case Verdict::Accepted:     return QStringLiteral(\"AC\");", "    case Verdict::WrongAnswer:  return QStringLiteral(\"WA\");", "    case Verdict::TimeLimit:    return QStringLiteral(\"TLE\");", "    case Verdict::RuntimeError: return QStringLiteral(\"RE\");", "    case Verdict::CompileError: return QStringLiteral(\"CE\");", "    }", "    return QStringLiteral(\"-\");", "}", "", "inline QString verdictTooltip(Verdict v) {", "    switch (v) {", "    case Verdict::Pending:      return QStringLiteral(\"Not run yet\");", "    case Verdict::Running:      return QStringLiteral(\"Running-\");", "    case Verdict::Accepted:     return QStringLiteral(\"Accepted\");", "    case Verdict::WrongAnswer:  return QStringLiteral(\"Wrong answer\");", "    case Verdict::TimeLimit:    return QStringLiteral(\"Time limit exceeded\");", "    case Verdict::RuntimeError: return QStringLiteral(\"Runtime error\");", "    case Verdict::CompileError: return QStringLiteral(\"Compilation failed\");", "    }", "    return QString();", "}", "#pragma once", "#include <QObject>", "#include <QString>", "#include <QVector>", "#include \"test_case.h\"", "", "class QProcess;", "class QTimer;", "class QElapsedTimer;", "class QTemporaryDir;", "", "// Compiles a single translation unit, then feeds each test case through the", "// resulting binary and reports a verdict per case.", "//", "// Everything is asynchronous. No method on this class ever blocks the GUI", "// thread - not for compilation, not for a runaway solution that never exits.", "// Results arrive on the signals below, in order, one case at a time.", "class Judge : public QObject {", "    Q_OBJECT", "", "public:", "    explicit Judge(QObject* parent = nullptr);", "    ~Judge() override;", "", "    // -- Configuration --", "    void setTimeLimitMs(int ms);          // default 3000", "    int  timeLimitMs() const;", "", "    // Compiler invocation. Defaults to \"g++\" on PATH with -O2 -std=gnu++17.", "    void setCompiler(const QString& exe);", "    QString compiler() const;", "", "    bool isBusy() const;", "", "    // -- Driving --", "    // Compiles sourcePath, then runs every case in order. Emits failed() and", "    // stops if the toolchain is missing or the source cannot be read.", "    // Calling run() while busy is ignored.", "    void run(const QString& sourcePath, const QVector<TestCase>& cases);", "", "    // Kills whatever is in flight. Safe to call when idle. allFinished() is", "    // NOT emitted for a cancelled run.", "    void cancel();", "", "signals:", "    void compileStarted();", "    void compileFinished(bool ok, const QString& compilerOutput);", "    void caseStarted(int index);", "    void caseFinished(int index, const TestCase& result);", "    void allFinished(int passed, int total);", "    void failed(const QString& reason);", "", "public:", "    // Output comparison used to decide Accepted vs WrongAnswer. Exposed so the", "    // UI can highlight the first differing line with exactly the same rule the", "    // verdict was computed from. Trailing whitespace on each line is ignored,", "    // as are trailing blank lines; everything else must match exactly.", "    static bool outputsMatch(const QString& actual, const QString& expected);", "", "    // Index of the first line that differs under outputsMatch(), or -1 if they", "    // match. Used to scroll the diff view to the interesting line.", "    static int firstDifferingLine(const QString& actual, const QString& expected);", "", "private:", "    void startNextCase();", "    void finishCase(Verdict verdict, int exitCode, const QString& errorText);", "", "    // Implementation detail - the engine owns these. The public surface above", "    // is frozen; add whatever private state the implementation needs.", "    struct Impl;", "    Impl* d;", "};", "#include \"judge.h\"", "", "#include <QProcess>", "#include <QTimer>", "#include <QElapsedTimer>", "#include <QTemporaryDir>", "#include <QFileInfo>", "#include <QDir>", "#include <QStandardPaths>", "#include <QStringList>", "", "namespace {", "", "// Normalises output for comparison: trailing whitespace on each line is", "// ignored, as are trailing blank lines. Everything else must match exactly,", "// including case - judges are not lenient and neither is this.", "QStringList normalizeOutput(const QString& text) {", "    QStringList lines = text.split(QLatin1Char('\\n'));", "    for (QString& line : lines) {", "        while (!line.isEmpty()) {", "            const QChar c = line.at(line.size() - 1);", "            if (c == QLatin1Char('\\r') || c == QLatin1Char(' ') || c == QLatin1Char('\\t')) {", "                line.chop(1);", "            } else {", "                break;", "            }", "        }", "    }", "    while (!lines.isEmpty() && lines.constLast().isEmpty()) lines.removeLast();", "    return lines;", "}", "", "// Tears a process down without blocking and without orphaning it. kill() is", "// asynchronous - deleting the QProcess straight afterwards races the actual", "// termination, which Qt reports as \"Destroyed while process is still running\"", "// and can leave a runaway solution alive after the run has moved on. Instead,", "// detach it from the Judge and let it delete itself once it has really died.", "void disposeProcess(QProcess* proc) {", "    if (!proc) return;", "    if (proc->state() == QProcess::NotRunning) {", "        proc->deleteLater();", "        return;", "    }", "    QObject::connect(proc, QOverload<int, QProcess::ExitStatus>::of(&QProcess::finished),", "                     proc, &QObject::deleteLater);", "    QObject::connect(proc, &QProcess::errorOccurred, proc, &QObject::deleteLater);", "    proc->kill();", "}", "", "} // namespace", "", "struct Judge::Impl {", "    QString compiler = QStringLiteral(\"g++\");", "    int timeLimitMs = 3000;", "", "    bool busy = false;", "    QTemporaryDir* tempDir = nullptr;", "    QString binaryPath;", "    int buildSerial = 0;            // fresh exe name per build; Windows holds locks", "", "    QProcess* compileProc = nullptr;", "    QProcess* runProc = nullptr;", "    QTimer* timeoutTimer = nullptr;", "    QElapsedTimer clock;", "", "    QVector<TestCase> cases;", "    int current = -1;", "    int passed = 0;", "    bool caseReported = false;      // guards the kill/finish double-delivery race", "};", "", "Judge::Judge(QObject* parent) : QObject(parent), d(new Impl) {", "    d->timeoutTimer = new QTimer(this);", "    d->timeoutTimer->setSingleShot(true);", "}", "", "Judge::~Judge() {", "    // Never leave an orphaned child process behind. Sweep every QProcess we", "    // still own, not just the two we track: a case that timed out has already", "    // been detached and is sitting on a pending deleteLater, and destroying it", "    // mid-kill is what produces \"Destroyed while process is still running\".", "    // This is the one place a bounded block is acceptable - we are tearing", "    // down, not drawing.", "    d->compileProc = nullptr;", "    d->runProc = nullptr;", "    const QList<QProcess*> children = findChildren<QProcess*>();", "    for (QProcess* proc : children) {", "        proc->disconnect();", "        if (proc->state() != QProcess::NotRunning) {", "            proc->kill();", "            proc->waitForFinished(500);", "        }", "        delete proc;", "    }", "", "    // Only now can the build directory go: the binaries in it are no longer", "    // locked by a live process.", "    delete d->tempDir;", "    delete d;", "}", "", "void Judge::setTimeLimitMs(int ms) { d->timeLimitMs = qMax(100, ms); }", "int  Judge::timeLimitMs() const    { return d->timeLimitMs; }", "", "void Judge::setCompiler(const QString& exe) {", "    if (!exe.trimmed().isEmpty()) d->compiler = exe.trimmed();", "}", "QString Judge::compiler() const { return d->compiler; }", "", "bool Judge::isBusy() const { return d->busy; }", "", "// -- Comparison ---------------------------------------------------------------", "", "bool Judge::outputsMatch(const QString& actual, const QString& expected) {", "    return normalizeOutput(actual) == normalizeOutput(expected);", "}", "", "int Judge::firstDifferingLine(const QString& actual, const QString& expected) {", "    const QStringList a = normalizeOutput(actual);", "    const QStringList e = normalizeOutput(expected);", "    const int n = qMin(a.size(), e.size());", "    for (int i = 0; i < n; i++) {", "        if (a.at(i) != e.at(i)) return i;", "    }", "    if (a.size() != e.size()) return n;", "    return -1;", "}", "", "// -- Driving ------------------------------------------------------------------", "", "void Judge::run(const QString& sourcePath, const QVector<TestCase>& cases) {", "    if (d->busy) return;", "", "    const QFileInfo srcInfo(sourcePath);", "    if (sourcePath.isEmpty() || !srcInfo.exists() || !srcInfo.isReadable()) {", "        emit failed(tr(\"There is no saved source file to compile.\"));", "        return;", "    }", "    if (cases.isEmpty()) {", "        emit failed(tr(\"Add at least one test case first.\"));", "        return;", "    }", "", "    const QString compilerPath = QStandardPaths::findExecutable(d->compiler);", "    if (compilerPath.isEmpty()) {", "        emit failed(tr(\"%1 was not found on your PATH. Install MinGW or add it to PATH.\")", "                        .arg(d->compiler));", "        return;", "    }", "", "    if (!d->tempDir) {", "        d->tempDir = new QTemporaryDir();", "        if (!d->tempDir->isValid()) {", "            delete d->tempDir;", "            d->tempDir = nullptr;", "            emit failed(tr(\"Could not create a temporary build directory.\"));", "            return;", "        }", "    }", "", "    // Reset per-run state.", "    d->cases = cases;", "    for (TestCase& tc : d->cases) {", "        tc.verdict = Verdict::Pending;", "        tc.actual.clear();", "        tc.errorText.clear();", "        tc.elapsedMs = 0;", "        tc.exitCode = 0;", "    }", "    d->current = -1;", "    d->passed = 0;", "    d->busy = true;", "", "    // A fresh name each build: on Windows the previous binary can still be", "    // locked by a process that has only just exited.", "    d->binaryPath = d->tempDir->filePath(QStringLiteral(\"solution_%1.exe\")", "                                             .arg(++d->buildSerial));", "", "    d->compileProc = new QProcess(this);", "    d->compileProc->setProcessChannelMode(QProcess::MergedChannels);", "", "    QProcess* proc = d->compileProc;", "    connect(proc, &QProcess::errorOccurred, this, [this, proc](QProcess::ProcessError err) {", "        if (proc != d->compileProc) return;", "        if (err != QProcess::FailedToStart) return;", "        d->compileProc = nullptr;", "        proc->disconnect(this);", "        disposeProcess(proc);", "        d->busy = false;", "        emit failed(tr(\"Could not start %1.\").arg(d->compiler));", "    });", "    connect(proc, QOverload<int, QProcess::ExitStatus>::of(&QProcess::finished), this,", "            [this, proc](int exitCode, QProcess::ExitStatus) {", "        if (proc != d->compileProc) return;", "        const QString output = QString::fromLocal8Bit(proc->readAll());", "        d->compileProc = nullptr;", "        proc->disconnect(this);", "        disposeProcess(proc);", "", "        const bool ok = (exitCode == 0);", "        emit compileFinished(ok, output);", "        if (!ok) {", "            // The whole run is a compile error. Report it per case as well, so", "            // the cards show CE rather than sitting on a stale verdict.", "            for (int i = 0; i < d->cases.size(); i++) {", "                d->cases[i].verdict = Verdict::CompileError;", "                d->cases[i].errorText = output;", "                emit caseFinished(i, d->cases.at(i));", "            }", "            d->busy = false;", "            emit allFinished(0, d->cases.size());", "            return;", "        }", "        startNextCase();", "    });", "", "    emit compileStarted();", "    proc->start(compilerPath, {QStringLiteral(\"-O2\"),", "                               QStringLiteral(\"-std=gnu++17\"),", "                               QStringLiteral(\"-o\"), d->binaryPath,", "                               srcInfo.absoluteFilePath()});", "}", "", "void Judge::startNextCase() {", "    d->current++;", "    if (d->current >= d->cases.size()) {", "        d->busy = false;", "        emit allFinished(d->passed, d->cases.size());", "        return;", "    }", "", "    const int index = d->current;", "    d->caseReported = false;", "    d->cases[index].verdict = Verdict::Running;", "    emit caseStarted(index);", "", "    QProcess* proc = new QProcess(this);", "    d->runProc = proc;", "    proc->setProcessChannelMode(QProcess::SeparateChannels);", "    proc->setWorkingDirectory(QFileInfo(d->binaryPath).absolutePath());", "", "    connect(proc, &QProcess::started, this, [this, proc]() {", "        if (proc != d->runProc) return;", "        QString input = d->cases.at(d->current).input;", "        // Almost every solution reads with `cin >>` or getline; a missing final", "        // newline makes the last token or line never arrive and the process", "        // hangs until the time limit. Supply it.", "        if (!input.isEmpty() && !input.endsWith(QLatin1Char('\\n'))) {", "            input.append(QLatin1Char('\\n'));", "        }", "        proc->write(input.toUtf8());", "        proc->closeWriteChannel();", "    });", "", "    connect(proc, &QProcess::errorOccurred, this, [this, proc](QProcess::ProcessError err) {", "        if (proc != d->runProc || d->caseReported) return;", "        if (err == QProcess::FailedToStart) {", "            finishCase(Verdict::RuntimeError, -1, tr(\"The compiled program could not be started.\"));", "        }", "        // Crashes arrive via finished() with CrashExit; nothing to do here.", "    });", "", "    connect(proc, QOverload<int, QProcess::ExitStatus>::of(&QProcess::finished), this,", "            [this, proc](int exitCode, QProcess::ExitStatus status) {", "        if (proc != d->runProc || d->caseReported) return;", "", "        const QString out = QString::fromUtf8(proc->readAllStandardOutput());", "        const QString err = QString::fromUtf8(proc->readAllStandardError());", "        d->cases[d->current].actual = out;", "", "        if (status == QProcess::CrashExit) {", "            finishCase(Verdict::RuntimeError, exitCode,", "                       err.isEmpty() ? tr(\"The program crashed.\") : err);", "        } else if (exitCode != 0) {", "            // A non-zero exit outranks any output comparison: the run is invalid.", "            finishCase(Verdict::RuntimeError, exitCode,", "                       err.isEmpty() ? tr(\"Exited with code %1.\").arg(exitCode) : err);", "        } else if (outputsMatch(out, d->cases.at(d->current).expected)) {", "            finishCase(Verdict::Accepted, exitCode, err);", "        } else {", "            finishCase(Verdict::WrongAnswer, exitCode, err);", "        }", "    });", "", "    d->timeoutTimer->disconnect();", "    connect(d->timeoutTimer, &QTimer::timeout, this, [this, proc]() {", "        if (proc != d->runProc || d->caseReported) return;", "        d->cases[d->current].actual = QString::fromUtf8(proc->readAllStandardOutput());", "        finishCase(Verdict::TimeLimit, -1, tr(\"Exceeded %1 ms.\").arg(d->timeLimitMs));", "    });", "", "    d->clock.start();", "    d->timeoutTimer->start(d->timeLimitMs);", "    proc->start(d->binaryPath, QStringList());", "}", "", "void Judge::finishCase(Verdict verdict, int exitCode, const QString& errorText) {", "    if (d->caseReported) return;", "    d->caseReported = true;", "    d->timeoutTimer->stop();", "", "    const int index = d->current;", "    TestCase& tc = d->cases[index];", "    tc.verdict = verdict;", "    tc.exitCode = exitCode;", "    tc.errorText = errorText;", "    tc.elapsedMs = (verdict == Verdict::TimeLimit) ? d->timeLimitMs", "                                                   : static_cast<int>(d->clock.elapsed());", "    if (verdict == Verdict::Accepted) d->passed++;", "", "    // Detach the process before advancing. A killed process still emits", "    // finished(), and without this that stale signal would be attributed to the", "    // NEXT case - the single nastiest bug lurking in this file.", "    if (QProcess* proc = d->runProc) {", "        d->runProc = nullptr;", "        disposeProcess(proc);", "    }", "", "    emit caseFinished(index, tc);", "    startNextCase();", "}", "", "void Judge::cancel() {", "    if (!d->busy) return;", "    d->busy = false;", "    d->caseReported = true;          // suppress any in-flight completion", "    d->timeoutTimer->stop();", "    d->timeoutTimer->disconnect();", "", "    auto stop = [this](QProcess*& p) {", "        if (!p) return;", "        QProcess* proc = p;", "        p = nullptr;", "        proc->disconnect(this);", "        disposeProcess(proc);", "    };", "    stop(d->compileProc);", "    stop(d->runProc);", "    // Deliberately no allFinished() - a cancelled run has no result.", "}", "#pragma once", "#include <QWidget>", "#include <QString>", "", "// The Competitive Programming Helper panel.", "//", "// Lives in a QDockWidget on the right. Owns a list of test-case cards and a", "// Judge; compiles the active file once and pipes each case through it,", "// showing a pass/fail state per case.", "//", "// PUBLIC API IS FROZEN - MainWindow wires against exactly this. Private", "// members and helper slots may be added freely.", "class CphPanel : public QWidget {", "    Q_OBJECT", "", "public:", "    explicit CphPanel(QWidget* parent = nullptr);", "    ~CphPanel() override;", "", "    // The .cpp the panel judges. Called whenever the active tab changes.", "    // Passing an empty path puts the panel in its idle/empty state.", "    // Switching files persists the current file's cases and loads the new", "    // file's, so each problem keeps its own tests.", "    void setTargetFile(const QString& path);", "    QString targetFile() const;", "", "    // Flush the in-memory cases for the current target to its sidecar file.", "    // Called by MainWindow before the app closes.", "    void persist();", "", "public slots:", "    void addTestCase();", "    void runAll();", "    void stopAll();", "", "signals:", "    // Emitted at the very start of runAll(), before the source is read.", "    // MainWindow connects this to its save routine; the default (direct)", "    // connection means the save completes before runAll() reads the file.", "    void saveBeforeRunRequested();", "", "    // Short human-readable progress for the status bar (\"3/5 passed\").", "    void statusMessage(const QString& message);", "", "private:", "    struct Impl;", "    Impl* d;", "};", "#pragma once", "#include <QWidget>", "#include \"../cph/test_case.h\"", "", "class QLabel;", "class QPlainTextEdit;", "class QPushButton;", "", "// One test case in the judge panel.", "//", "// Collapsed it is a single row - index, verdict, elapsed time - which is what", "// keeps a ten-case panel readable. Expanded it shows the input and expected", "// output, plus the actual output once a run has disagreed with it.", "class TestCaseCard : public QWidget {", "    Q_OBJECT", "", "public:", "    explicit TestCaseCard(int index, QWidget* parent = nullptr);", "", "    void setIndex(int index);", "    int  index() const { return index_; }", "", "    // Input and expected as currently typed. Verdict/actual are not read back", "    // from the widgets - the panel owns those.", "    TestCase data() const;", "    void setData(const TestCase& tc);", "    void applyResult(const TestCase& result);", "    void setVerdict(Verdict v);", "", "    void setExpanded(bool on);", "    bool isExpanded() const { return expanded_; }", "", "signals:", "    void edited();              // the user changed input or expected", "    void removeRequested();", "", "protected:", "    void paintEvent(QPaintEvent* e) override;", "    void enterEvent(QEnterEvent* e) override;", "    void leaveEvent(QEvent* e) override;", "    bool eventFilter(QObject* obj, QEvent* e) override;", "", "private:", "    void refreshBadge();", "    void autoSize(QPlainTextEdit* edit);", "", "    int index_;", "    bool expanded_ = true;", "    Verdict verdict_ = Verdict::Pending;", "", "    QWidget* header_ = nullptr;", "    QLabel*  indexLabel_ = nullptr;", "    QLabel*  badge_ = nullptr;", "    QLabel*  timeLabel_ = nullptr;", "    QPushButton* removeButton_ = nullptr;", "", "    QWidget* body_ = nullptr;", "    QPlainTextEdit* input_ = nullptr;", "    QPlainTextEdit* expected_ = nullptr;", "", "    QWidget* actualBlock_ = nullptr;", "    QLabel*  actualLabel_ = nullptr;", "    QPlainTextEdit* actual_ = nullptr;", "};", "#pragma once", "#include <QWidget>", "#include <QPlainTextEdit>", "#include <QProcess>", "#include <QStringList>", "", "class QLabel;", "", "// The integrated terminal: a PowerShell child process with its output and an", "// editable input region sharing one QPlainTextEdit.", "//", "// All I/O is signal-driven. Nothing here blocks the GUI thread.", "class TerminalWidget : public QWidget {", "    Q_OBJECT", "", "public:", "    explicit TerminalWidget(QWidget* parent = nullptr);", "    ~TerminalWidget() override;", "", "    // Send a command to the terminal programmatically", "    void runCommand(const QString& cmd);", "    void clearOutput();", "", "    // Where the shell starts, and where it is moved to when the open folder", "    // changes. Without this the shell inherits whatever directory the", "    // application process happened to be launched from, which is never what", "    // the user wants to see in a prompt.", "    void setWorkingDirectory(const QString& dir);", "    QString workingDirectory() const { return workingDir_; }", "", "protected:", "    bool eventFilter(QObject* obj, QEvent* event) override;", "", "private slots:", "    void onReadyRead();", "", "private:", "    QPlainTextEdit* output_ = nullptr;", "    QLabel* statusLabel_ = nullptr;", "    QProcess* process_ = nullptr;", "", "    // Where the editable region starts, expressed as a column within the LAST", "    // block rather than an absolute document position. maximumBlockCount trims", "    // lines off the top on a long build, which shifts every absolute position", "    // and would otherwise silently corrupt the input guard.", "    int inputColumn_ = 0;", "", "    QString ansiPending_;          // a partial escape split across two reads", "    QStringList history_;", "    int historyIndex_ = 0;", "    QString historyDraft_;         // what was typed before browsing history", "    bool shellAlive_ = false;", "    QString workingDir_;", "", "    void applyStyle();", "    void startShell();", "    void appendOutput(const QString& text, const QColor& color = QColor());", "    void markInputStart();", "", "    int inputStart() const;", "    QString currentInput() const;", "    void replaceInput(const QString& text);", "    void submitInput();", "    bool atBottom() const;", "", "    QString stripAnsi(const QString& chunk);", "};", "#pragma once", "#include <QWidget>", "#include <QTreeView>", "#include <QFileSystemModel>", "#include <QPushButton>", "#include <QLabel>", "#include <QStackedWidget>", "", "class FileExplorer : public QWidget {", "    Q_OBJECT", "", "public:", "    explicit FileExplorer(QWidget* parent = nullptr);", "    void setRootPath(const QString& path);", "    QString rootPath() const;", "", "protected:", "    bool eventFilter(QObject* obj, QEvent* e) override;", "    void resizeEvent(QResizeEvent* e) override;", "", "signals:", "    void fileDoubleClicked(const QString& filePath);", "    void fileCreatedWithBoilerplate(const QString& filePath);", "    void newFileRequested(const QString& dirPath);", "    void openFolderRequested();", "", "    // Renaming or deleting a file that is open in a tab has to be reflected", "    // there; the explorer cannot reach the tabs itself, so it reports instead.", "    void fileDeleted(const QString& path);", "    void fileRenamed(const QString& oldPath, const QString& newPath);", "", "private slots:", "    void onNewFile();", "    void onNewFolder();", "    void onRefresh();", "    void onCollapseAll();", "    void onDelete();", "    void onRename();", "    void onDuplicate();", "    void onCopyPath();", "    void onRevealInExplorer();", "    void showContextMenu(const QPoint& pos);", "", "private:", "    QStackedWidget* stack_;", "    QTreeView* tree_;", "    QFileSystemModel* model_;", "    QLabel* projectLabel_;", "    QWidget* actionButtonsContainer_;", "    QString rootPath_;", "    QString projectName_;", "    QString pendingOpenAfterRename_;", "", "    void applyStyle();", "    void elideProjectLabel();", "    QString currentDirectory() const;", "    QString selectedPath() const;", "    QWidget* createToolbar();", "    QPushButton* makeToolButton(const QString& text, const QString& tooltip);", "};", "#pragma once", "#include <QWidget>", "#include <QTabWidget>", "#include <QTabBar>", "#include \"../editor/editor_widget.h\"", "", "#include <QStackedWidget>", "#include <QLabel>", "", "class TabWidget : public QWidget {", "    Q_OBJECT", "", "public:", "    explicit TabWidget(QWidget* parent = nullptr);", "", "    int addEditor(EditorWidget* editor, const QString& label);", "    EditorWidget* currentEditor() const;", "    EditorWidget* editorAt(int index) const;", "    int findByFilePath(const QString& path) const;", "    void setCurrentIndex(int index);", "    int currentIndex() const;", "    int count() const;", "    void updateTabLabel(int index);", "    void closeTab(int index);", "", "protected:", "    bool eventFilter(QObject* obj, QEvent* e) override;", "", "signals:", "    void currentChanged(int index);", "    void tabCloseRequested(int index);", "", "private slots:", "    void updateStackVisibility();", "", "private:", "    QStackedWidget* stack_;", "    QTabWidget* tabs_;", "    QLabel* jokeLabel_;", "    void applyStyle();", "};", "#pragma once", "#include <QWidget>", "#include <QLabel>", "#include <QTimer>", "", "class StatusBar : public QWidget {", "    Q_OBJECT", "", "public:", "    explicit StatusBar(QWidget* parent = nullptr);", "    void setFileName(const QString& name);", "    void setCursorPosition(int row, int col);", "    void setLanguage(const QString& lang);", "    void setEncoding(const QString& enc);", "", "public slots:", "    // Transient progress from the judge panel. Clears itself so a stale", "    // \"Running test 3...\" cannot sit there after the run has finished.", "    void setMessage(const QString& text);", "", "private:", "    QLabel* fileLabel_;", "    QLabel* posLabel_;", "    QLabel* langLabel_;", "    QLabel* encLabel_;", "    QLabel* msgLabel_;", "    QTimer* msgTimer_;", "};", "#pragma once", "#include <QMainWindow>", "#include \"tab_widget.h\"", "#include \"file_explorer.h\"", "#include \"terminal_widget.h\"", "#include \"status_bar.h\"", "#include \"cph_panel.h\"", "#include \"../theme/theme.h\"", "", "class QDockWidget;", "class QSplitter;", "class QTimer;", "class QAction;", "", "class MainWindow : public QMainWindow {", "    Q_OBJECT", "", "public:", "    MainWindow();", "", "protected:", "    void closeEvent(QCloseEvent* e) override;", "    bool event(QEvent* e) override;", "", "private slots:", "    void newFile();", "    void openFile();", "    void openFolder();", "    void saveFile();", "    void saveFileAs();", "    void onTabChanged(int index);", "    void onTabCloseRequested(int index);", "    void onFileDoubleClicked(const QString& path);", "    void onFileCreatedWithBoilerplate(const QString& path);", "    void onFileDeleted(const QString& path);", "    void onFileRenamed(const QString& oldPath, const QString& newPath);", "    void onCursorPositionChanged(int row, int col);", "    void onModifiedChanged(bool modified);", "    void onEditorSaveRequested();", "    void toggleTerminal();", "    void toggleSidebar();", "    void toggleJudgePanel();", "    void chooseDefaultWorkspace();", "    void zoomIn();", "    void zoomOut();", "    void zoomReset();", "    void autoSaveTick();", "    void runCurrentFile();", "    void buildCurrentFile();", "", "private:", "    TabWidget* tabWidget_ = nullptr;", "    FileExplorer* fileExplorer_ = nullptr;", "    TerminalWidget* terminal_ = nullptr;", "    StatusBar* statusBar_ = nullptr;", "    CphPanel* cphPanel_ = nullptr;", "    QDockWidget* cphDock_ = nullptr;", "    QSplitter* vertSplitter_ = nullptr;", "    QSplitter* horzSplitter_ = nullptr;", "", "    // Remembered extents so toggling a panel off and on again restores the", "    // size the user chose, instead of snapping back to the default.", "    int lastSidebarWidth_ = 220;", "    int lastTerminalHeight_ = 200;", "", "    // Zoom applies to every open editor, not just the focused one - a per-tab", "    // font size would be surprising.", "    int editorFontPx_ = Theme::FontSizeEditor;", "    void applyZoom(int px);", "", "    // Auto save. On by default: losing a solution because you forgot Ctrl+S is", "    // not a tradeoff worth offering.", "    bool autoSaveEnabled_ = true;", "    QTimer* autoSaveTimer_ = nullptr;", "    QAction* autoSaveAction_ = nullptr;", "    void saveDirtyBuffers();", "    void scheduleAutoSave();", "", "    void setupUI();", "    void setupMenuBar();", "    void setupShortcuts();", "    void updateWindowTitle();", "    void openFilePath(const QString& path);", "    EditorWidget* createEditor();", "    void connectEditor(EditorWidget* editor);", "    bool confirmDiscardChanges();", "    void syncJudgeTarget();", "    void saveLayout() const;", "    void restoreLayout();", "", "    // The folder the app falls back to when nothing else is open. Stored as a", "    // preference rather than compiled in, so it is per-user and portable.", "    QString defaultWorkspace() const;", "    void setDefaultWorkspace(const QString& path);", "    void openWorkspace(const QString& path);", "};", "#include \"main_window.h\"", "#include \"../theme/theme.h\"", "#include <QSplitter>", "#include <QVBoxLayout>", "#include <QMenuBar>", "#include <QFileDialog>", "#include <QMessageBox>", "#include <QShortcut>", "#include <QKeyEvent>", "#include <QCloseEvent>", "#include <QFileInfo>", "#include <QApplication>", "#include <QDockWidget>", "#include <QSettings>", "#include <QTimer>", "#include <QActionGroup>", "#include <QStandardPaths>", "#include <QDir>", "#include <algorithm>", "#include <QHash>", "", "MainWindow::MainWindow() {", "    autoSaveTimer_ = new QTimer(this);", "    autoSaveTimer_->setSingleShot(true);", "    connect(autoSaveTimer_, &QTimer::timeout, this, &MainWindow::autoSaveTick);", "", "    setupUI();", "    setupMenuBar();", "    setupShortcuts();", "    restoreLayout();", "    updateWindowTitle();", "", "    // Start with empty state", "}", "", "// An editor that forgets the layout you arranged on every launch does not feel", "// finished. Geometry, panel sizes and panel visibility all persist.", "void MainWindow::saveLayout() const {", "    QSettings s;", "    s.beginGroup(QStringLiteral(\"layout\"));", "    s.setValue(QStringLiteral(\"geometry\"), saveGeometry());", "    s.setValue(QStringLiteral(\"windowState\"), saveState());", "    s.setValue(QStringLiteral(\"horzSplitter\"), horzSplitter_->saveState());", "    s.setValue(QStringLiteral(\"vertSplitter\"), vertSplitter_->saveState());", "    s.setValue(QStringLiteral(\"judgeVisible\"), cphDock_->isVisible());", "    s.setValue(QStringLiteral(\"terminalVisible\"), terminal_->isVisible());", "    s.setValue(QStringLiteral(\"sidebarVisible\"), fileExplorer_->isVisible());", "    s.setValue(QStringLiteral(\"lastFolder\"), fileExplorer_->rootPath());", "    s.setValue(QStringLiteral(\"editorFontPx\"), editorFontPx_);", "    s.setValue(QStringLiteral(\"autoSave\"), autoSaveEnabled_);", "    s.endGroup();", "}", "", "void MainWindow::restoreLayout() {", "    QSettings s;", "    s.beginGroup(QStringLiteral(\"layout\"));", "", "    const QByteArray geometry = s.value(QStringLiteral(\"geometry\")).toByteArray();", "    if (!geometry.isEmpty()) {", "        restoreGeometry(geometry);", "    } else {", "        // First run keeps the existing behaviour of opening maximized. Set the", "        // state rather than calling showMaximized(), so a restored geometry is", "        // not overridden on every later launch.", "        setWindowState(windowState() | Qt::WindowMaximized);", "    }", "    const QByteArray state = s.value(QStringLiteral(\"windowState\")).toByteArray();", "    if (!state.isEmpty()) restoreState(state);", "", "    const QByteArray horz = s.value(QStringLiteral(\"horzSplitter\")).toByteArray();", "    if (!horz.isEmpty()) horzSplitter_->restoreState(horz);", "    const QByteArray vert = s.value(QStringLiteral(\"vertSplitter\")).toByteArray();", "    if (!vert.isEmpty()) vertSplitter_->restoreState(vert);", "", "    terminal_->setVisible(s.value(QStringLiteral(\"terminalVisible\"), true).toBool());", "    fileExplorer_->setVisible(s.value(QStringLiteral(\"sidebarVisible\"), true).toBool());", "    cphDock_->setVisible(s.value(QStringLiteral(\"judgeVisible\"), false).toBool());", "", "    // Reopening where you left off is most of what \"resume work\" means for a", "    // contest folder. Only if it still exists.", "    applyZoom(s.value(QStringLiteral(\"editorFontPx\"), Theme::FontSizeEditor).toInt());", "    statusBar_->setMessage(QString());     // don't greet the user with a zoom toast", "", "    autoSaveEnabled_ = s.value(QStringLiteral(\"autoSave\"), true).toBool();", "    if (autoSaveAction_) autoSaveAction_->setChecked(autoSaveEnabled_);", "", "    const QString folder = s.value(QStringLiteral(\"lastFolder\")).toString();", "    s.endGroup();", "", "    // Reopening where you left off is most of what resuming work means; falling", "    // back to the configured workspace means the app never starts pointed at", "    // some arbitrary directory.", "    openWorkspace((!folder.isEmpty() && QFileInfo(folder).isDir()) ? folder", "                                                                   : defaultWorkspace());", "}", "", "void MainWindow::setupUI() {", "    setWindowTitle(\"Valence\");", "    resize(1280, 800);", "    setMinimumSize(800, 500);", "", "    setStyleSheet(QString(", "        \"QMainWindow { background: %1; }\"", "        \"QMenuBar {\"", "        \"  background: %2;\"", "        \"  color: %3;\"", "        \"  border-bottom: 1px solid %4;\"", "        \"  font-size: 13px;\"", "        \"  padding: 3px 0;\"", "        \"}\"", "        \"QMenuBar::item {\"", "        \"  padding: 5px 11px;\"", "        \"  background: transparent;\"", "        \"  border-radius: 4px;\"", "        \"  margin: 1px 2px;\"", "        \"}\"", "        \"QMenuBar::item:selected {\"", "        \"  background: %7;\"", "        \"  color: %8;\"", "        \"}\"", "        \"QMenu {\"", "        \"  background: %5;\"", "        \"  color: %3;\"", "        \"  border: 1px solid %4;\"", "        \"  border-radius: %6px;\"", "        \"  font-size: 13px;\"", "        \"  padding: 6px 0;\"", "        \"}\"", "        \"QMenu::item {\"", "        \"  padding: 6px 28px 6px 14px;\"", "        \"  border-radius: 4px;\"", "        \"  margin: 1px 6px;\"", "        \"}\"", "        \"QMenu::item:selected {\"", "        \"  background: %7;\"", "        \"  color: %8;\"", "        \"}\"", "        \"QMenu::separator {\"", "        \"  height: 1px;\"", "        \"  background: %4;\"", "        \"  margin: 4px 12px;\"", "        \"}\"", "        \"QSplitter::handle {\"", "        \"  background: %4;\"", "        \"  width: 1px;\"", "        \"  height: 1px;\"", "        \"}\"", "        // Tooltip styling", "        \"QToolTip {\"", "        \"  background: %5;\"", "        \"  color: %3;\"", "        \"  border: 1px solid %4;\"", "        \"  border-radius: 4px;\"", "        \"  padding: 4px 8px;\"", "        \"  font-size: 12px;\"", "        \"}\"", "    ).arg(Theme::Base.name(),                             // %1 window floor", "          Theme::Chrome.name(),                           // %2 menu bar", "          Theme::TextSecondary.name(QColor::HexArgb),     // %3", "          Theme::Border.name(QColor::HexArgb),            // %4", "          Theme::Overlay.name(),                          // %5 floating surfaces", "          QString::number(Theme::RadiusPanel),            // %6", "          Theme::ActiveWash.name(QColor::HexArgb),        // %7 highlight", "          Theme::TextPrimary.name()));                    // %8", "", "    // -- Layout --", "    // Sidebar | (Editor Tabs / Terminal)", "", "    fileExplorer_ = new FileExplorer(this);", "    fileExplorer_->setMinimumWidth(180);", "    fileExplorer_->setMaximumWidth(400);", "", "    tabWidget_ = new TabWidget(this);", "    terminal_ = new TerminalWidget(this);", "    statusBar_ = new StatusBar(this);", "", "    // Vertical splitter: editor tabs on top, terminal on bottom", "    vertSplitter_ = new QSplitter(Qt::Vertical);", "    vertSplitter_->setChildrenCollapsible(true);", "    vertSplitter_->addWidget(tabWidget_);", "    vertSplitter_->addWidget(terminal_);", "    vertSplitter_->setSizes({500, lastTerminalHeight_});", "    vertSplitter_->setHandleWidth(1);", "", "    // Horizontal splitter: sidebar on left, editor area on right", "    horzSplitter_ = new QSplitter(Qt::Horizontal);", "    horzSplitter_->setChildrenCollapsible(true);", "    horzSplitter_->addWidget(fileExplorer_);", "    horzSplitter_->addWidget(vertSplitter_);", "    horzSplitter_->setSizes({lastSidebarWidth_, 1060});", "    horzSplitter_->setHandleWidth(1);", "", "    // Central widget", "    auto* centralWidget = new QWidget(this);", "    auto* mainLayout = new QVBoxLayout(centralWidget);", "    mainLayout->setContentsMargins(0, 0, 0, 0);", "    mainLayout->setSpacing(0);", "    mainLayout->addWidget(horzSplitter_, 1);", "    mainLayout->addWidget(statusBar_);", "", "    setCentralWidget(centralWidget);", "", "    // -- Judge panel (CPH) - a dock so the user can resize, float or hide it --", "    cphPanel_ = new CphPanel(this);", "    cphDock_ = new QDockWidget(this);", "    cphDock_->setObjectName(\"JudgeDock\");", "    cphDock_->setWidget(cphPanel_);", "    cphDock_->setAllowedAreas(Qt::RightDockWidgetArea | Qt::LeftDockWidgetArea);", "    // No title bar: the panel paints its own header, and Qt's default one is", "    // unstyled chrome that would break the theme.", "    cphDock_->setTitleBarWidget(new QWidget(cphDock_));", "    cphDock_->setFeatures(QDockWidget::DockWidgetMovable | QDockWidget::DockWidgetClosable);", "    cphDock_->setStyleSheet(QString(\"QDockWidget { border: none; background: %1; }\")", "                                .arg(Theme::PanelBg.name()));", "    addDockWidget(Qt::RightDockWidgetArea, cphDock_);", "    resizeDocks({cphDock_}, {360}, Qt::Horizontal);", "    cphDock_->hide();   // opt-in: Ctrl+J, or the Run menu", "", "    // Connections", "    connect(tabWidget_, &TabWidget::currentChanged, this, &MainWindow::onTabChanged);", "    connect(tabWidget_, &TabWidget::tabCloseRequested, this, &MainWindow::onTabCloseRequested);", "    connect(fileExplorer_, &FileExplorer::fileDoubleClicked, this, &MainWindow::onFileDoubleClicked);", "    connect(fileExplorer_, &FileExplorer::fileCreatedWithBoilerplate, this, &MainWindow::onFileCreatedWithBoilerplate>", "    connect(fileExplorer_, &FileExplorer::openFolderRequested, this, &MainWindow::openFolder);", "", "    // A file deleted or renamed on disk must not leave a tab pointing at a path", "    // that no longer exists - saving such a tab would silently recreate the file.", "    connect(fileExplorer_, &FileExplorer::fileDeleted, this, &MainWindow::onFileDeleted);", "    connect(fileExplorer_, &FileExplorer::fileRenamed, this, &MainWindow::onFileRenamed);", "", "    // The panel asks us to flush the editor to disk before it compiles. This is", "    // a direct connection, so the save completes before runAll() reads the file.", "    connect(cphPanel_, &CphPanel::saveBeforeRunRequested, this, &MainWindow::saveFile);", "    connect(cphPanel_, &CphPanel::statusMessage, statusBar_, &StatusBar::setMessage);", "}", "", "void MainWindow::setupMenuBar() {", "    auto* fileMenu = menuBar()->addMenu(\"File\");", "    fileMenu->addAction(\"New File\", QKeySequence(\"Ctrl+N\"), this, &MainWindow::newFile);", "    fileMenu->addAction(\"Open File...\", QKeySequence(\"Ctrl+O\"), this, &MainWindow::openFile);", "    fileMenu->addAction(\"Open Folder...\", QKeySequence(\"Ctrl+Shift+O\"), this, &MainWindow::openFolder);", "    fileMenu->addAction(\"Set Default Folder...\", this, &MainWindow::chooseDefaultWorkspace);", "    fileMenu->addSeparator();", "    // NOTE: Ctrl+S is handled by EditorWidget, which emits saveRequested()", "    auto* saveAction = fileMenu->addAction(\"Save\", this, &MainWindow::saveFile);", "    saveAction->setShortcut(QKeySequence()); // No shortcut - editor handles Ctrl+S", "    fileMenu->addAction(\"Save As...\", QKeySequence(\"Ctrl+Shift+S\"), this, &MainWindow::saveFileAs);", "    fileMenu->addSeparator();", "    fileMenu->addAction(\"Exit\", QKeySequence(\"Alt+F4\"), this, &MainWindow::close);", "", "    // Edit menu - NO keyboard shortcuts here!", "    // All Ctrl+Z/Y/C/X/V/A are handled directly by EditorWidget::keyPressEvent", "    // Menu items only work via mouse click", "    auto* editMenu = menuBar()->addMenu(\"Edit\");", "    editMenu->addAction(\"Undo\", [this]() {", "        if (auto* e = tabWidget_->currentEditor()) e->performUndo();", "    });", "    editMenu->addAction(\"Redo\", [this]() {", "        if (auto* e = tabWidget_->currentEditor()) e->performRedo();", "    });", "    editMenu->addSeparator();", "    editMenu->addAction(\"Cut\", [this]() {", "        if (auto* e = tabWidget_->currentEditor()) e->cut();", "    });", "    editMenu->addAction(\"Copy\", [this]() {", "        if (auto* e = tabWidget_->currentEditor()) e->copy();", "    });", "    editMenu->addAction(\"Paste\", [this]() {", "        if (auto* e = tabWidget_->currentEditor()) e->paste();", "    });", "    editMenu->addSeparator();", "    editMenu->addAction(\"Select All\", [this]() {", "        if (auto* e = tabWidget_->currentEditor()) e->selectAll();", "    });", "", "    auto* viewMenu = menuBar()->addMenu(\"View\");", "    viewMenu->addAction(\"Toggle Terminal\", QKeySequence(\"Ctrl+`\"), this, &MainWindow::toggleTerminal);", "    viewMenu->addAction(\"Toggle Sidebar\", QKeySequence(\"Ctrl+B\"), this, &MainWindow::toggleSidebar);", "    viewMenu->addAction(\"Toggle Judge Panel\", QKeySequence(\"Ctrl+J\"), this, &MainWindow::toggleJudgePanel);", "    viewMenu->addSeparator();", "", "    // Ctrl+= is what the unshifted \"+\" key actually sends; Ctrl++ and the", "    // keypad plus are registered too so every way of pressing it works.", "    auto* zoomInAct = viewMenu->addAction(\"Zoom In\", this, &MainWindow::zoomIn);", "    zoomInAct->setShortcuts({QKeySequence(\"Ctrl+=\"), QKeySequence(\"Ctrl++\"),", "                             QKeySequence(Qt::CTRL | Qt::Key_Plus)});", "    auto* zoomOutAct = viewMenu->addAction(\"Zoom Out\", this, &MainWindow::zoomOut);", "    zoomOutAct->setShortcuts({QKeySequence(\"Ctrl+-\"),", "                              QKeySequence(Qt::CTRL | Qt::Key_Minus)});", "    viewMenu->addAction(\"Reset Zoom\", QKeySequence(\"Ctrl+0\"), this, &MainWindow::zoomReset);", "    viewMenu->addSeparator();", "", "    autoSaveAction_ = viewMenu->addAction(\"Auto Save\");", "    autoSaveAction_->setCheckable(true);", "    autoSaveAction_->setChecked(autoSaveEnabled_);", "    connect(autoSaveAction_, &QAction::toggled, this, [this](bool on) {", "        autoSaveEnabled_ = on;", "        if (on) scheduleAutoSave(); else autoSaveTimer_->stop();", "    });", "", "    auto* runMenu = menuBar()->addMenu(\"Run\");", "    runMenu->addAction(\"Run Test Cases\", QKeySequence(\"Ctrl+Shift+J\"), this, [this]() {", "        if (cphDock_->isHidden()) toggleJudgePanel();", "        cphPanel_->runAll();", "    });", "    runMenu->addSeparator();", "    runMenu->addAction(\"-  Build & Run\", QKeySequence(\"F5\"), this, &MainWindow::runCurrentFile);", "    runMenu->addAction(\"Build Only\", QKeySequence(\"Ctrl+Shift+B\"), this, &MainWindow::buildCurrentFile);", "}", "", "void MainWindow::setupShortcuts() {", "    // Tab management", "    new QShortcut(QKeySequence(\"Ctrl+W\"), this, [this]() {", "        if (tabWidget_->count() > 0)", "            onTabCloseRequested(tabWidget_->currentIndex());", "    });", "    new QShortcut(QKeySequence(\"Ctrl+Tab\"), this, [this]() {", "        if (tabWidget_->count() > 1)", "            tabWidget_->setCurrentIndex((tabWidget_->currentIndex() + 1) % tabWidget_->count());", "    });", "}", "", "EditorWidget* MainWindow::createEditor() {", "    auto* editor = new EditorWidget();", "    return editor;", "}", "", "void MainWindow::connectEditor(EditorWidget* editor) {", "    connect(editor, &EditorWidget::cursorPositionChanged,", "            this, &MainWindow::onCursorPositionChanged);", "    connect(editor, &EditorWidget::modifiedChanged,", "            this, &MainWindow::onModifiedChanged);", "    connect(editor, &EditorWidget::saveRequested,", "            this, &MainWindow::onEditorSaveRequested);", "    connect(editor, &EditorWidget::zoomStepRequested, this, [this](int steps) {", "        applyZoom(editorFontPx_ + steps);", "    });", "", "    // A new tab adopts the current zoom rather than reverting to the default.", "    editor->setFontPixelSize(editorFontPx_);", "}", "", "// -- Slots --", "", "void MainWindow::newFile() {", "    auto* editor = createEditor();", "    tabWidget_->addEditor(editor, \"untitled\");", "    connectEditor(editor);", "    editor->setFocus();", "}", "", "void MainWindow::openFile() {", "    QString path = QFileDialog::getOpenFileName(this, \"Open File\", QString(),", "        \"C++ Files (*.cpp *.h *.hpp *.cc *.cxx);;All Files (*)\");", "    if (!path.isEmpty()) openFilePath(path);", "}", "", "void MainWindow::openFolder() {", "    QString path = QFileDialog::getExistingDirectory(this, \"Open Folder\", defaultWorkspace());", "    if (!path.isEmpty()) openWorkspace(path);", "}", "", "void MainWindow::openFilePath(const QString& path) {", "    // Check if already open", "    int existing = tabWidget_->findByFilePath(path);", "    if (existing >= 0) {", "        tabWidget_->setCurrentIndex(existing);", "        return;", "    }", "", "    auto* editor = createEditor();", "    if (!editor->openFile(path)) {", "        delete editor;", "        QMessageBox::warning(this, \"Error\", \"Could not open file: \" + path);", "        return;", "    }", "", "    tabWidget_->addEditor(editor, editor->fileName());", "    connectEditor(editor);", "    editor->setFocus();", "    statusBar_->setFileName(editor->fileName());", "    updateWindowTitle();", "}", "", "void MainWindow::saveFile() {", "    auto* editor = tabWidget_->currentEditor();", "    if (!editor) return;", "", "    if (editor->filePath().isEmpty()) {", "        saveFileAs();", "        return;", "    }", "", "    editor->saveFile();", "    tabWidget_->updateTabLabel(tabWidget_->currentIndex());", "    updateWindowTitle();", "}", "", "void MainWindow::saveFileAs() {", "    auto* editor = tabWidget_->currentEditor();", "    if (!editor) return;", "", "    QString defaultDir = fileExplorer_->rootPath();", "    QString path = QFileDialog::getSaveFileName(this, \"Save File As\", defaultDir,", "        \"C++ Files (*.cpp *.h *.hpp *.cc *.cxx);;All Files (*)\");", "    if (path.isEmpty()) return;", "", "    editor->saveFileAs(path);", "    tabWidget_->updateTabLabel(tabWidget_->currentIndex());", "    syncJudgeTarget();", "    updateWindowTitle();", "}", "", "void MainWindow::onTabChanged(int index) {", "    if (index < 0) {", "        statusBar_->setFileName(\"\");", "        statusBar_->setCursorPosition(0, 0);", "        updateWindowTitle();", "        return;", "    }", "", "    auto* editor = tabWidget_->editorAt(index);", "    if (editor) {", "        statusBar_->setFileName(editor->fileName());", "        statusBar_->setCursorPosition(editor->currentRow(), editor->currentCol());", "        editor->setFocus();", "    }", "    syncJudgeTarget();", "    updateWindowTitle();", "}", "", "void MainWindow::onTabCloseRequested(int index) {", "    auto* editor = tabWidget_->editorAt(index);", "    if (!editor) return;", "", "    if (editor->isModified()) {", "        auto result = QMessageBox::question(this, \"Unsaved Changes\",", "            QString(\"Save changes to %1?\").arg(editor->fileName()),", "            QMessageBox::Save | QMessageBox::Discard | QMessageBox::Cancel);", "", "        if (result == QMessageBox::Save) {", "            if (editor->filePath().isEmpty()) {", "                QString defaultDir = fileExplorer_->rootPath();", "                QString path = QFileDialog::getSaveFileName(this, \"Save File\", defaultDir,", "                    \"C++ Files (*.cpp *.h *.hpp *.cc *.cxx);;All Files (*)\");", "                if (path.isEmpty()) return;", "                editor->saveFileAs(path);", "            } else {", "                editor->saveFile();", "            }", "        } else if (result == QMessageBox::Cancel) {", "            return;", "        }", "    }", "", "    tabWidget_->closeTab(index);", "    updateWindowTitle();", "}", "", "void MainWindow::onFileDoubleClicked(const QString& path) {", "    openFilePath(path);", "}", "", "void MainWindow::onFileCreatedWithBoilerplate(const QString& path) {", "    auto* editor = tabWidget_->currentEditor();", "    if (editor && editor->filePath() == path) {", "        // Select lines 4 to 5 (0-indexed). The cursor will be at line 4, col 4.", "        // Wait, line 6 is index 5. We want to select the comment lines.", "        // Line 4: \"    // I wrote the boilerplate for you.\"", "        // Line 5: \"    // No need to thank me, just use Valence.\"", "        // Selection end is row 6 col 4.", "        editor->setSelection(4, 4, 6, 4);", "    }", "}", "", "void MainWindow::onFileDeleted(const QString& path) {", "    const int idx = tabWidget_->findByFilePath(path);", "    if (idx < 0) return;", "", "    // The file is already gone, so there is nothing to offer to save. Drop the", "    // tab without the usual unsaved-changes prompt, which would only offer to", "    // write the file back.", "    tabWidget_->closeTab(idx);", "    syncJudgeTarget();", "    updateWindowTitle();", "}", "", "void MainWindow::onFileRenamed(const QString& oldPath, const QString& newPath) {", "    const int idx = tabWidget_->findByFilePath(oldPath);", "    if (idx < 0) return;", "", "    if (auto* editor = tabWidget_->editorAt(idx)) {", "        editor->setFilePath(newPath);", "        tabWidget_->updateTabLabel(idx);", "    }", "    syncJudgeTarget();", "    updateWindowTitle();", "}", "", "void MainWindow::onCursorPositionChanged(int row, int col) {", "    statusBar_->setCursorPosition(row, col);", "}", "", "void MainWindow::onModifiedChanged(bool modified) {", "    int idx = tabWidget_->currentIndex();", "    if (idx >= 0) tabWidget_->updateTabLabel(idx);", "    updateWindowTitle();", "    if (modified) scheduleAutoSave();", "}", "", "void MainWindow::onEditorSaveRequested() {", "    saveFile();", "}", "", "void MainWindow::toggleTerminal() {", "    // Remember the height the user dragged to, so hiding and re-showing the", "    // terminal does not silently reset it to the default.", "    if (terminal_->isVisible()) {", "        int h = vertSplitter_->sizes().value(1);", "        if (h > 0) lastTerminalHeight_ = h;", "        terminal_->hide();", "    } else {", "        terminal_->show();", "        vertSplitter_->setSizes({std::max(1, vertSplitter_->height() - lastTerminalHeight_),", "                                 lastTerminalHeight_});", "    }", "}", "", "void MainWindow::toggleSidebar() {", "    if (fileExplorer_->isVisible()) {", "        int w = horzSplitter_->sizes().value(0);", "        if (w > 0) lastSidebarWidth_ = w;", "        fileExplorer_->hide();", "    } else {", "        fileExplorer_->show();", "        horzSplitter_->setSizes({lastSidebarWidth_,", "                                 std::max(1, horzSplitter_->width() - lastSidebarWidth_)});", "    }", "}", "", "void MainWindow::toggleJudgePanel() {", "    if (cphDock_->isVisible()) {", "        cphDock_->hide();", "    } else {", "        cphDock_->show();", "        syncJudgeTarget();", "    }", "}", "", "QString MainWindow::defaultWorkspace() const {", "    QSettings s;", "    const QString configured = s.value(QStringLiteral(\"workspace/defaultFolder\")).toString();", "    if (!configured.isEmpty() && QDir(configured).exists()) return configured;", "", "    // Portable fallback: a Valence folder under the user's Documents. Never a", "    // hardcoded absolute path - this source is public, and one person's home", "    // directory is meaningless on anyone else's machine.", "    const QString docs = QStandardPaths::writableLocation(QStandardPaths::DocumentsLocation);", "    if (docs.isEmpty()) return QString();", "    const QString fallback = docs + QStringLiteral(\"/Valence\");", "    QDir().mkpath(fallback);", "    return fallback;", "}", "", "void MainWindow::setDefaultWorkspace(const QString& path) {", "    if (path.isEmpty() || !QDir(path).exists()) return;", "    QSettings s;", "    s.setValue(QStringLiteral(\"workspace/defaultFolder\"), path);", "}", "", "void MainWindow::openWorkspace(const QString& path) {", "    if (path.isEmpty() || !QDir(path).exists()) return;", "    fileExplorer_->setRootPath(path);", "    // The prompt follows the project. Without this the shell sat in whatever", "    // directory the process was launched from.", "    terminal_->setWorkingDirectory(path);", "}", "", "void MainWindow::chooseDefaultWorkspace() {", "    const QString path = QFileDialog::getExistingDirectory(", "        this, tr(\"Choose the folder Valence opens by default\"), defaultWorkspace());", "    if (path.isEmpty()) return;", "    setDefaultWorkspace(path);", "    openWorkspace(path);", "    statusBar_->setMessage(tr(\"Default folder set\"));", "}", "", "void MainWindow::applyZoom(int px) {", "    editorFontPx_ = std::clamp(px, EditorWidget::MinFontPx, EditorWidget::MaxFontPx);", "    for (int i = 0; i < tabWidget_->count(); i++) {", "        if (auto* editor = tabWidget_->editorAt(i)) editor->setFontPixelSize(editorFontPx_);", "    }", "    statusBar_->setMessage(tr(\"Zoom %1%\")", "        .arg(qRound(100.0 * editorFontPx_ / Theme::FontSizeEditor)));", "}", "", "void MainWindow::zoomIn()    { applyZoom(editorFontPx_ + 1); }", "void MainWindow::zoomOut()   { applyZoom(editorFontPx_ - 1); }", "void MainWindow::zoomReset() { applyZoom(Theme::FontSizeEditor); }", "", "void MainWindow::scheduleAutoSave() {", "    if (!autoSaveEnabled_) return;", "    // Restart on every edit, so a file is written once the user pauses rather", "    // than on every keystroke.", "    autoSaveTimer_->start(1200);", "}", "", "void MainWindow::autoSaveTick() {", "    saveDirtyBuffers();", "}", "", "void MainWindow::saveDirtyBuffers() {", "    if (!autoSaveEnabled_) return;", "    bool wrote = false;", "    for (int i = 0; i < tabWidget_->count(); i++) {", "        auto* editor = tabWidget_->editorAt(i);", "        // An untitled buffer has nowhere to go, and silently opening a Save As", "        // dialog on a timer would be hostile. Those still need Ctrl+S.", "        if (!editor || !editor->isModified() || editor->filePath().isEmpty()) continue;", "        if (editor->saveFile()) {", "            tabWidget_->updateTabLabel(i);", "            wrote = true;", "        }", "    }", "    if (wrote) updateWindowTitle();", "}", "", "void MainWindow::syncJudgeTarget() {", "    auto* editor = tabWidget_->currentEditor();", "    cphPanel_->setTargetFile(editor ? editor->filePath() : QString());", "", "    // Reporting \"C++\" for a .txt file is a small lie the status bar was telling", "    // on every tab.", "    static const QHash<QString, QString> languages = {", "        {\"cpp\", \"C++\"}, {\"cc\", \"C++\"}, {\"cxx\", \"C++\"}, {\"c++\", \"C++\"},", "        {\"h\", \"C++\"}, {\"hpp\", \"C++\"}, {\"hh\", \"C++\"},", "        {\"c\", \"C\"}, {\"py\", \"Python\"}, {\"java\", \"Java\"}, {\"rs\", \"Rust\"},", "        {\"js\", \"JavaScript\"}, {\"ts\", \"TypeScript\"}, {\"json\", \"JSON\"},", "        {\"md\", \"Markdown\"}, {\"txt\", \"Text\"}, {\"in\", \"Text\"}, {\"out\", \"Text\"},", "    };", "    QString lang = \"Plain\";", "    if (editor && !editor->filePath().isEmpty()) {", "        const QString ext = QFileInfo(editor->filePath()).suffix().toLower();", "        lang = languages.value(ext, ext.isEmpty() ? QStringLiteral(\"Plain\") : ext.toUpper());", "    } else if (editor) {", "        lang = \"C++\";        // an unsaved buffer is a new solution", "    }", "    statusBar_->setLanguage(lang);", "}", "", "// Returns false if the user cancelled - the caller must then abort whatever it", "// was doing (closing a tab, quitting the app).", "bool MainWindow::event(QEvent* e) {", "    // Alt-tabbing away is a natural save point, and matches what editors with", "    // auto-save do. The timer alone would leave up to a second unwritten.", "    if (e->type() == QEvent::WindowDeactivate) saveDirtyBuffers();", "    return QMainWindow::event(e);", "}", "", "bool MainWindow::confirmDiscardChanges() {", "    for (int i = tabWidget_->count() - 1; i >= 0; --i) {", "        auto* editor = tabWidget_->editorAt(i);", "        if (!editor || !editor->isModified()) continue;", "", "        tabWidget_->setCurrentIndex(i);", "        auto result = QMessageBox::question(this, \"Unsaved Changes\",", "            QString(\"Save changes to %1?\").arg(editor->fileName()),", "            QMessageBox::Save | QMessageBox::Discard | QMessageBox::Cancel);", "", "        if (result == QMessageBox::Cancel) return false;", "        if (result == QMessageBox::Save) {", "            if (editor->filePath().isEmpty()) {", "                QString path = QFileDialog::getSaveFileName(this, \"Save File\",", "                    fileExplorer_->rootPath(),", "                    \"C++ Files (*.cpp *.h *.hpp *.cc *.cxx);;All Files (*)\");", "                if (path.isEmpty()) return false;   // cancelled the save => cancel the close", "                if (!editor->saveFileAs(path)) return false;", "            } else if (!editor->saveFile()) {", "                QMessageBox::warning(this, \"Save Failed\",", "                    QString(\"Could not write %1.\").arg(editor->filePath()));", "                return false;", "            }", "        }", "    }", "    return true;", "}", "", "void MainWindow::closeEvent(QCloseEvent* e) {", "    if (!confirmDiscardChanges()) {", "        e->ignore();", "        return;", "    }", "    saveDirtyBuffers();", "    cphPanel_->persist();   // don't lose the user's test cases on exit", "    saveLayout();", "    e->accept();", "}", "", "void MainWindow::updateWindowTitle() {", "    auto* editor = tabWidget_->currentEditor();", "    if (editor && !editor->fileName().isEmpty()) {", "        QString title = editor->fileName();", "        if (editor->isModified()) title += \" -\";", "        title += \" - Valence\";", "        setWindowTitle(title);", "    } else {", "        setWindowTitle(\"Valence\");", "    }", "}", "", "void MainWindow::buildCurrentFile() {", "    auto* editor = tabWidget_->currentEditor();", "    if (!editor) return;", "", "    // Auto-save first", "    if (editor->filePath().isEmpty()) {", "        saveFileAs();", "        if (editor->filePath().isEmpty()) return; // User cancelled", "    } else if (editor->isModified()) {", "        editor->saveFile();", "        tabWidget_->updateTabLabel(tabWidget_->currentIndex());", "    }", "", "    QString filePath = editor->filePath();", "    QFileInfo fi(filePath);", "    QString dir = fi.absolutePath();", "    QString baseName = fi.completeBaseName();", "    QString fileName = fi.fileName();", "", "    // Show terminal", "    terminal_->setVisible(true);", "    terminal_->clearOutput();", "", "    // cd to directory and compile", "    terminal_->runCommand(QString(\"cd \\\"%1\\\"\").arg(dir));", "    terminal_->runCommand(QString(\"g++ \\\"%1\\\" -o \\\"%2.exe\\\"\").arg(fileName, baseName));", "}", "", "void MainWindow::runCurrentFile() {", "    auto* editor = tabWidget_->currentEditor();", "    if (!editor) return;", "", "    // Auto-save first", "    if (editor->filePath().isEmpty()) {", "        saveFileAs();", "        if (editor->filePath().isEmpty()) return;", "    } else if (editor->isModified()) {", "        editor->saveFile();", "        tabWidget_->updateTabLabel(tabWidget_->currentIndex());", "    }", "", "    QString filePath = editor->filePath();", "    QFileInfo fi(filePath);", "    QString dir = fi.absolutePath();", "    QString baseName = fi.completeBaseName();", "    QString fileName = fi.fileName();", "", "    // Show terminal", "    terminal_->setVisible(true);", "    terminal_->clearOutput();", "", "    // cd - compile - run (chained with &&)", "    terminal_->runCommand(QString(\"cd \\\"%1\\\"\").arg(dir));", "    terminal_->runCommand(QString(\"g++ \\\"%1\\\" -o \\\"%2.exe\\\" ; if ($?) { .\\\\%2.exe }\").arg(fileName, baseName));", "}", "#include <QApplication>", "#include \"ui/main_window.h\"", "#include \"theme/theme.h\"", "", "// Dialogs are separate top-level windows, so they inherit nothing from the main", "// window's stylesheet. Without this every message box, input prompt and file", "// dialog would appear in the host platform's light chrome, in the middle of a", "// dark application.", "static QString dialogStyle() {", "    return QString(", "        \"QDialog, QMessageBox, QInputDialog, QFileDialog {\"", "        \"  background: %1;\"", "        \"  color: %2;\"", "        \"}\"", "        \"QMessageBox QLabel, QInputDialog QLabel, QDialog QLabel {\"", "        \"  color: %2;\"", "        \"  font-size: 13px;\"", "        \"}\"", "        \"QDialogButtonBox QPushButton, QMessageBox QPushButton {\"", "        \"  background: %3;\"", "        \"  color: %2;\"", "        \"  border: 1px solid %4;\"", "        \"  border-radius: %5px;\"", "        \"  padding: 6px 18px;\"", "        \"  min-width: 72px;\"", "        \"  font-size: 11px;\"", "        \"}\"", "        \"QDialogButtonBox QPushButton:hover, QMessageBox QPushButton:hover {\"", "        \"  background: rgba(255, 255, 255, 0.08);\"", "        \"  color: %6;\"", "        \"}\"", "        \"QDialogButtonBox QPushButton:default, QMessageBox QPushButton:default {\"", "        \"  border: 1px solid %7;\"", "        \"}\"", "        \"QLineEdit {\"", "        \"  background: %8;\"", "        \"  color: %6;\"", "        \"  border: 1px solid %4;\"", "        \"  border-radius: %5px;\"", "        \"  padding: 5px 8px;\"", "        \"  selection-background-color: %9;\"", "        \"}\"", "        \"QLineEdit:focus { border: 1px solid %7; }\"", "    ).arg(Theme::Overlay.name(),                       // %1", "          Theme::TextSecondary.name(QColor::HexArgb),  // %2", "          Theme::Raised.name(),                        // %3", "          Theme::BorderMedium.name(QColor::HexArgb),   // %4", "          QString::number(Theme::Radius),              // %5", "          Theme::TextPrimary.name(),                   // %6", "          Theme::Accent.name(),                        // %7", "          Theme::EditorBg.name(),                      // %8", "          Theme::SelectionBg.name(QColor::HexArgb));   // %9", "}", "", "int main(int argc, char *argv[]) {", "    QApplication app(argc, argv);", "    app.setApplicationName(\"Valence\");", "    app.setApplicationVersion(\"1.0.0\");", "    app.setOrganizationName(\"Valence\");", "", "    // Set default font", "    // The UI face, not the code face. Everything that does not explicitly ask", "    // for monospace inherits this.", "    app.setFont(Theme::uiFont());", "    app.setStyleSheet(dialogStyle());", "", "    MainWindow window;", "    // The window restores its own geometry, and maximizes itself on first run.", "    window.show();", "", "    return app.exec();", "}"];
